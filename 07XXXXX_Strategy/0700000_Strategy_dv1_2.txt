DocDCN:     0700000
DocTitle:   Strategy
DocVersion: dv1_2
DocOwner:   Bill
DocPath:    GitHub.com/TothBot/TothBot_V2-Docs/07XXXXX_Strategy/0700000_Strategy_dv1_2.txt
DocDate:    05-17-2026
DocTime:    23:59:59 UTC

============================================================
STRATEGY
============================================================

============================================================
1. PURPOSE
============================================================

This document is the Domain Document for AA=07 Strategy.
It defines the entire Strategy domain: its purpose, scope,
governing principles, hard rules, and the complete
structure of all controlled documents within it.

The Strategy domain governs every rule, model, threshold,
and decision that determines WHICH assets TothBot V2
monitors, WHEN a trade signal is generated, and WHAT
market regime conditions allow or block entry.

Nothing that touches signal generation, regime
classification, or asset universe definition is
unspecified. Nothing is assumed. Everything is explicitly
defined in this domain or in the instance documents
beneath it.

============================================================
2. SCOPE
============================================================

The Strategy domain owns:

  - The SSS (Simple Signal System) signal model: the
    complete three-factor momentum scoring model operating
    on 5-minute OHLC candles
  - All indicator definitions, computation formulas,
    seeding procedures, and incremental update rules
    for RSI(14), EMA(9), EMA(21), and Volume MA(20)
  - The regime taxonomy: the 6-regime classification
    grid based on daily ADX(14), EMA(20/50), and ATR(14)
    percentile rank
  - All regime computation rules including data source,
    timing, response[-1] exclusion, and entry blocking
    rules per regime
  - The asset selection universe: the criteria for which
    Kraken spot pairs TothBot monitors and trades
  - BTC/USD anchor status as the market_regime proxy
  - All CIATS-owned strategy parameters and their
    starting values

The Strategy domain does NOT own:

  - Selection Controller gate logic (AA=08):
    Gate 5 (SSS), Gate 6 (Regime Gate) use Strategy
    outputs. AA=08 owns the gate execution logic.
  - Position sizing or R:R formula (AA=06)
  - Order dispatch and entry mechanics (AA=08)
  - Exchange wire protocol (AA=04)
  - CIATS improvement cycle procedures (AA=02)
  - System component architecture (AA=05)

The boundary is signal and regime definition. The Strategy
domain produces the signal and regime state. The Trading
System domain (AA=08) consumes them. The domains are
independent specifications. Neither owns the other's
content.

============================================================
3. GOVERNING PRINCIPLES
============================================================

3.1 Loss Prevention Takes Priority Over Profit Generation

Every strategy rule is designed first to avoid entering
bad trades, second to capture good ones. Regime blocking
(Gate 3) exists to prevent entries in adverse market
conditions. The SSS thresholds exist to require confirmed
momentum before entry, not to maximize signal frequency.
When signal quality and signal frequency conflict, quality
wins.

3.2 All Strategy Parameters Are CIATS-Owned Starting Values

Every strategy parameter — RSI thresholds, EMA periods,
volume multiplier, ADX threshold, ATR percentile, universe
size — is a starting value derived from FP/DP analysis.
None are sacred. CIATS replaces every starting value with
data after the 200-trade floor is reached. PDCA governs
all changes. The only permanent strategy decision is the
three-factor SSS structure and the 6-regime grid topology.
The values inside them are CIATS property.

3.3 Signal Must Align Across Timeframes

The SSS signal is validated by multi-timeframe alignment:
  - Daily regime (Gate 3): trend direction and volatility
  - 1H HTF (Gate 4): 1H EMA(20) > EMA(50) confirmation
  - 5-minute SSS (Gate 5): entry timeframe signal

All three timeframes must be aligned before a trade is
generated. A strong 5-minute signal in a bad regime is
rejected. This is the core structural principle of the
strategy.

Per 0500000 dv1_80 D7 Dispatch Seam: Long_Module and
Short_Module both consume the same gate output via the
dispatch-seam routing layer. Multi-timeframe alignment
is evaluated once per pipeline pass; the dispatch seam
routes the aligned signal to the appropriate module
(direction-dependent).

3.4 No Indicator Reimplementation Without Specification

Indicator formulas are exactly specified in this domain.
RSI uses Wilder's SMMA (AR-076) — NOT standard EMA. This
distinction produces 5-8% variance in RSI values and
would systematically mis-fire the SSS RSI gate if
implemented incorrectly. The specifications in this domain
are the coding contract. Deviations require an AR and a
document update before code changes.

3.5 The Asset Universe Is Dynamic, Not Static

The monitored pair set is populated at startup and updated
in real time from the Kraken instrument channel. CIATS
may expand or contract the universe after 200 trades.
BTC/USD is permanently anchored regardless of CIATS
decisions — it is the market_regime proxy for all trades.

3.6 Paper-As-Master Principle Reference

All AA=07 Strategy parameter values are paper-canonical
per TB00000 §4.4 PA-001..PA-010 PAPER-AS-MASTER PRINCIPLE
(enacted v2_18). The full set of CIATS-owned parameters
governed by this domain (SSS Signal Parameters + Regime
Parameters + Asset Universe Parameters) evolves through
CIATS PDCA cycles operating on paper-trading data. Per
PA-007: parameter values validated by paper-trading
empirical data are the canonical values for live
operation. Live trading mimics paper for the four
PA-004 enumerated divergence points (private WS
connection, order dispatch, capital ledger, position
mirror sourcing). Outside those four points, strategy-
domain behavior is byte-identical across paper and live.

============================================================
4. STRATEGY ARCHITECTURE SUMMARY
============================================================

TothBot V2 strategy operates on three layers:

------------------------------------------------------------
4.1 Asset Universe Layer (0711003)
------------------------------------------------------------

Determines WHICH pairs TothBot monitors. Populated at
startup from the Kraken instrument channel. Criteria:
  (1) Quote currency: USD, USDC, or USDT
      (per TB00128 Ratified Decision D-03 LOCKED)
  (2) Instrument status: online
  (3) 24h volume: >= min_volume_usd_daily ($500k starting)

Starting universe: ALL Kraken USD/USDC/USDT pairs that
satisfy criteria (1)–(3). NO Top-N cap (per TB00128
Ratified Decision D-08 LOCKED — top-N cap retired).
BTC/USD always included regardless of volume ranking.
Universe is live: updated when instrument channel fires
status changes. CIATS may adjust universe after 200 trades.

------------------------------------------------------------
4.2 Regime Classification Layer (0711002)
------------------------------------------------------------

Determines WHICH market conditions allow entry. Computed
daily at 00:00 UTC for every monitored pair via REST
GetOHLCData (daily interval). One call per pair per day.

Six regimes on a 2x3 grid:
  Directional state (rows):
    TRENDING_POSITIVE:  ADX(14) > 25 AND EMA(20) > EMA(50)
    TRENDING_NEGATIVE:  ADX(14) > 25 AND EMA(20) < EMA(50)
    NON_DIRECTIONAL:    ADX(14) <= 25

  Volatility state (columns):
    NORMAL_VOL:         ATR(14) percentile <= 67th
    ELEVATED_VOL:       ATR(14) percentile > 67th

Entry is permitted in two of six regimes:
  TRENDING_POS + NORMAL_VOL:   FULL position
  NON_DIR + NORMAL_VOL:        50% position (Gate 6 sizes)
All other regimes: Gate 3 blocks entry.

Every trade is tagged with both asset_regime and
market_regime (BTC/USD proxy) for CIATS analysis.

------------------------------------------------------------
4.3 SSS Signal Engine Layer (0711001)
------------------------------------------------------------

Determines WHEN to enter a monitored pair. Runs on every
5-minute OHLC candle close after Regime (Gate 3) and HTF
(Gate 4) pass. Three factors must all be true. Per
0500000 dv1_80 lines 5435-5436 + 5463-5464 + 6117
parallel-module framework (Path 2 RSI parameter rename
per TB00179 STAGE M-2 enactment), SSS conditions are
direction-aware:

  SC-SSS-1 (long):  rsi_long_low (30) < RSI(14) on
                    5-min < rsi_long_high (50)
  SC-SSS-1 (short): rsi_short_high (50) < RSI(14) on
                    5-min < rsi_short_low (70)

  SC-SSS-2 (long):  EMA(9) > EMA(21) on 5-min
  SC-SSS-2 (short): EMA(9) < EMA(21) on 5-min

  SC-SSS-3 (shared): Volume > Volume_MA(20) x 1.0x

All three conditions are required simultaneously. A pass
on two of three is a fail. SSS output is a signal_params
dict logged to CIATS Trade Outcome Bus on every evaluation,
pass or fail. Direction (long vs short) is determined by
the parallel-module dispatch-seam (0500000 dv1_80 D7);
Long_Module consumes the long-direction SC-SSS-1 + SC-
SSS-2 evaluations and Short_Module consumes the short-
direction evaluations. SC-SSS-3 is shared (direction-
independent).

============================================================
5. HARD RULES — STRATEGY DOMAIN
============================================================

HR-STRAT-001  Strategy signals are generated only on
              5-minute OHLC candle close events. Intra-
              candle evaluation is prohibited. The candle
              close is the system clock.

HR-STRAT-002  SSS requires ALL THREE conditions
              simultaneously. Two-of-three is a fail.
              Partial credit does not exist.

HR-STRAT-003  RSI(14) MUST use Wilder's SMMA (AR-076).
              Standard EMA (alpha=2/15) is PROHIBITED for
              RSI computation. EMA(9) and EMA(21) use
              standard EMA. Only RSI uses Wilder's SMMA.

HR-STRAT-004  GetOHLCData response[-1] MUST be excluded
              from ALL regime calculations (GAP-03, AR-017).
              The last entry is always the not-yet-committed
              current candle. Including it corrupts all
              regime indicators.

HR-STRAT-005  Regime is computed from daily candles only
              (GetOHLCData interval=1440 or equivalent).
              The 1H EMA (Gate 4) uses the ohlc(60) stream.
              The 5-minute SSS uses the ohlc(5) stream.
              These are three separate data sources.
              Never conflate timeframes.

HR-STRAT-006  BTC/USD is permanently in the monitored
              universe. It cannot be removed by CIATS.
              It is the market_regime proxy. Every trade
              carries market_regime from the BTC/USD
              regime computation at trade entry time.

HR-STRAT-007  WARM_UP to READY transition requires ALL
              FIVE indicators seeded per pair (ATR(14),
              RSI(14), EMA(9), EMA(21), Volume MA(20)).
              A pair is not tradeable until all five are
              seeded. ATR-only seeding is insufficient.

HR-STRAT-008  CIATS parameter changes require the 200-trade
              floor. No strategy parameter may be changed
              by CIATS below 200 closed trades. EWMA
              monitoring is active below 200 trades but
              does not drive strategy parameter changes.

HR-STRAT-009  Signal params are always logged, pass or
              fail. The signal_params dict is written to
              the Trade Outcome Bus on every SSS evaluation
              regardless of result. This is CIATS data.
              No evaluation is silent.

HR-STRAT-010  Strategy domain does not own the Selection
              Controller gate execution logic. 0711001
              defines what the SSS signal IS. 0811002
              defines the gate that evaluates it. They
              are separate specifications. Never combine
              signal definition with gate logic.

============================================================
6. STRATEGY PARAMETER SUMMARY
============================================================

The following parameters are CIATS-owned starting values.
All are initial values derived from FP/DP analysis with
5+ independent sources. All will be replaced by data
after the 200-trade floor is reached. Per TB00000 v2_50
§9.16 parameter philosophy + §9.17 CIATS PARAMETER
REGISTRY, parameters are instantiated per-module under
the v2_29 parallel-module framework (Long_Module +
Short_Module each get own parameter set). TB00000 §9.17
is the canonical registry; the layout below mirrors
that registry for the AA=07 Strategy-domain subset.

  Long-side SSS signal model (Long_Module only,
  0711001):
    rsi_long_low:           30  (RSI > 30 = oversold
                                exit; long entry zone
                                start)
    rsi_long_high:          50  (RSI < 50 = momentum
                                entry zone end; mirror
                                of rsi_short_high=50)

  Short-side SSS signal model (Short_Module only,
  0711001):
    rsi_short_low:          70  (oversold-short
                                equivalent; mirrors
                                long-side rsi_long_low
                                = 30)
    rsi_short_high:         50  (mirrors long-side
                                rsi_long_high = 50)
    leverage_cap_short:     3x  (Kraken native max is
                                10x; starting low per
                                loss-minimization
                                principle; CIATS-
                                adjustable)
    min_order_size_usd:     Kraken per-pair minimum

  Common SSS signal model (Long_Module + Short_Module,
  0711001):
    sss_ema_short:          9   (EMA short period for
                                SSS Gate 2; same value
                                both modules; per-
                                module CIATS may diverge
                                from 200-trade threshold
                                forward)
    sss_ema_long:           21  (EMA long period for
                                SSS Gate 2; same value
                                both modules)
    volume_sss_threshold:   1.0x (volume vs MA(20) for
                                SSS volume confirmation;
                                same value both modules)

  Regime Parameters (per-module instantiated, 0711002):
    adx_threshold:          25   (trending vs non-
                                 directional)
    htf_ema_short:          20   (daily EMA short
                                 period)
    htf_ema_long:           50   (daily EMA long
                                 period)
    atr_percentile_thresh:  67th (normal vs elevated
                                 vol)
    atr_rolling_window:     50   (days for percentile
                                 rank)

  Asset Universe Parameters (0711003):
    monitored_universe_n:   RETIRED per TB00128 D-08
                            LOCKED (no top-N cap; full
                            universe)
    min_volume_usd_daily:   $500k (minimum 24h volume
                            filter)

  Liquidity cache (universal — same value applies to
  the single REST Ticker refresh cycle, not per-module;
  0411002 REST-TKR-007):
    liquidity_refresh_hours: 4 hours starting (CIATS-
                            adjustable per REST-TKR-007;
                            cache TTL on
                            channel:kraken_rest_Ticker
                            liquidity probe at universe
                            load + refresh cycle)

The one non-CIATS strategy decision: three-factor SSS
structure and 6-regime grid topology. These are
architectural decisions, not tunable parameters. The
net 1:1.5 R:R minimum floor (TB00000 §9.10 SACRED) is
also non-CIATS-owned but is governed by AA=06 Risk,
not by this Strategy domain.

============================================================
7. RELATIONSHIP TO TRADING PIPELINE
============================================================

The 8-gate trading pipeline (0500000 Section 3) operates
on every 5-minute OHLC candle close. Strategy owns the
content evaluated at three gates:

  Gate 1  State Machine     — system readiness
  Gate 2  Liquidity         — 24h volume check (0711003)
  Gate 3  Regime Pre-Filter — regime blocks (0711002)
  Gate 4  1H HTF            — 1H EMA alignment (0711002)
  Gate 5  SSS Signal Engine — 3-factor signal (0711001)
  Gate 6  Selection Ctrl    — quality gates (AA=08)
  Gate 7  Risk Guard        — capital check (AA=06)
  Gate 8  Position Sizer    — compute size (AA=06)

Strategy domain content is evaluated at Gates 2, 3, 4,
and 5. Strategy defines the rules. Trading System (AA=08)
and Risk (AA=06) execute them. The domains do not overlap.

============================================================
8. DOCUMENT STRUCTURE
============================================================

The Strategy domain contains one sub-domain:

  Sub-Domain 1 — Strategy_Signals (B=1)
    Anchor: 0710000 Strategy_Signals_Anchor
    Instance documents:
      0711001  SSS_Signal_Model_Specification
      0711002  Regime_Taxonomy_Specification
      0711003  Asset_Selection_Specification

------------------------------------------------------------
8.1 Sub-Domain Descriptions
------------------------------------------------------------

0711001  SSS_Signal_Model_Specification
  Complete specification of the Simple Signal System.
  Covers three SSS conditions, all indicator formulas,
  RSI Wilder's SMMA formula (AR-076), EMA standard formula,
  Volume MA formula, seeding procedures for all five
  indicators (AR-068), WARM_UP to READY transition rule,
  incremental update rules, and signal_params dict output.

0711002  Regime_Taxonomy_Specification
  Complete specification of the 6-regime classification
  grid. Covers directional state computation (ADX(14) +
  EMA(20/50) daily), volatility state computation (ATR(14)
  percentile 50-day rolling), data source (REST
  GetOHLCData daily), timing (00:00 UTC daily), response
  [-1] exclusion requirement (GAP-03), entry blocking
  rules per regime, two-level trade tagging (asset_regime
  + market_regime), and BTC/USD proxy role.

0711003  Asset_Selection_Specification
  Complete specification of the monitored pair universe.
  Covers selection criteria (USD/USDC/USDT quote, online
  status, volume threshold), full-universe inclusion
  (no top-N cap per D-08 LOCKED), BTC/USD anchor rule
  (AR-074), startup population procedure (instrument
  channel snapshot), live update procedure (instrument
  channel status events), and CIATS universe adjustment
  procedure.

------------------------------------------------------------
8.2 Document Chain Requirement
------------------------------------------------------------

  Instance Document -> 0710000 (Anchor) -> 0700000 (Domain)

This chain is mandatory and unbroken. No instance document
in AA=07 may be committed to GitHub without both 0710000
and 0700000 already present and approved on GitHub.

============================================================
9. RELATIONSHIPS TO OTHER DOMAINS
============================================================

  AA=05 System_Architecture_Overview (0500000)
    0500000 Sections 3 (pipeline), 4 (regime taxonomy),
    and the AR entries AR-067 through AR-076 are the
    primary source of record for all decisions captured
    in this domain. 0500000 references; AA=07 specifies.

  AA=08 Trading_System
    The Selection Controller (0811002) evaluates SSS
    output at Gate 5. The Long Module (0811001) dispatches
    entries for SSS-passing pairs. Gate 3 (Regime Pre-
    Filter) and Gate 4 (1H HTF) in the pipeline consume
    regime state produced by this domain. AA=07 defines
    the signal and regime state. AA=08 consumes them.

  AA=04 Exchange_Interface
    GetOHLCData REST call (daily interval) is the regime
    data source (AR-017, 0411002). The ohlc(5) and
    ohlc(60) WS streams are the SSS and HTF data sources
    (0411001). Exchange Interface defines the wire
    protocol. Strategy defines what is computed from it.

  AA=06 Risk
    Gate 7 (Risk Guard) and Gate 8 (Position Sizer)
    receive pipeline flow only after Gates 3-5 pass.
    Strategy domain does not own or reference risk
    parameters. Risk does not own or reference signal
    parameters. One-way dependency: Risk receives flow
    from Strategy pipeline output.

  AA=02 Operations / CIATS (0211001)
    CIATS owns all strategy parameters in the Parameter
    Store (except structural decisions). CIATS evaluates
    signal quality metrics from the Trade Outcome Bus.
    Strategy defines the parameters. CIATS evolves their
    values through PDCA after the 200-trade floor.

============================================================
10. OPEN ITEMS
============================================================

  None at dv1_2 (post-housekeeping).

  All strategy architectural decisions are fully resolved
  in 0500000 dv1_80 (AR-067 through AR-076 verified
  canonical at dv1_80 per TB00215 v1_0 Phase 1 grep-
  verify §5 OUTCOME I). All signal model parameters are
  defined per the parallel-module framework (TB00000
  v2_50 §9.17). All regime rules are locked. All asset
  selection criteria are specified. The dv1_1 -> dv1_2
  housekeeping amendment (TB00321 enactment under
  DEC-024 ratification) closed GD-1 through GD-11
  governance-drift findings per TB00215 v1_0 §6.2
  scope.

============================================================
11. RELATED DOCUMENTS
============================================================

  0000000  Ethics_and_Safety            (root Domain Document)
  0500000  System_Architecture_Overview (primary source;
                                        current canonical
                                        version dv1_80)
  0710000  Strategy_Signals_Anchor      (Sub-Domain 1 Anchor)
  0711001  SSS_Signal_Model_Specification
  0711002  Regime_Taxonomy_Specification
  0711003  Asset_Selection_Specification
  0411001  Kraken_WS_v2_Specification   (ohlc stream source)
  0411002  Kraken_REST_API_Specification (GetOHLCData source)
  0511002  Signal_Pipeline_Specification (pipeline consumer)
  0311001  Namespace_Specification      (governs DCN structure)
  0311002  Document_Creation_Workflow   (governs sequence)

  Retired AA=10 coding specs (NO LONGER LIVE
  IMPLEMENTATIONS — historical reference only):
    1011003  Signal_Pipeline_Coding_Spec  (RETIRED at
                                          TB00224 under
                                          DD-012 0500000-
                                          canonical-all-
                                          docs retirement
                                          cascade)
    1011012  Regime_Engine_Coding_Spec    (RETIRED at
                                          TB00227 under
                                          same DD-012
                                          cascade)

  Per 1010000 dv1_19 §4 retirement registry, both
  coding specs were retired during the DEC-012 cross-
  document 0500000-canonical-all-docs campaign. AA=10
  implementation for Strategy-domain signal pipeline +
  regime engine resolves to the canonical 0500000
  dv1_80 + AA=07 Sub-Domain 1 specification set.

============================================================
12. REVISION HISTORY
============================================================

  dv1_2  05-17-2026  TB00321 HOUSEKEEPING AMENDMENT
                     EXECUTION — nine-CHANGE-block scope
                     closing GD-1 through GD-11 governance-
                     drift findings per TB00215 v1_0 §6.2
                     enumeration under DEC-024 SUB-DECISION
                     Option B separate-session execution
                     methodology ratified at TB00320
                     chat-turn 1.

                     SCOPE OF CHANGES (nine surgical edits):

                     CHANGE 1 — §4.3 SC-SSS-1 update to
                     parallel-module form. Replaced single-
                     direction "RSI(14) on 5-min > 50
                     AND < 70" with two sub-rule form per
                     0500000 dv1_80 lines 5435-5436 +
                     5463-5464: SC-SSS-1 (long) rsi_long_
                     low (30) < RSI(14) < rsi_long_high
                     (50); SC-SSS-1 (short) rsi_short_high
                     (50) < RSI(14) < rsi_short_low (70).
                     Closes GD-1 HIGH (RSI parameter range
                     inversion vs canonical) + part of
                     GD-7 (per-rule direction encoding).

                     CHANGE 2 — §4.3 SC-SSS-2 + SC-SSS-3
                     parallel-module form. SC-SSS-2 (long):
                     EMA(9) > EMA(21). SC-SSS-2 (short):
                     EMA(9) < EMA(21). SC-SSS-3 (shared):
                     Volume > Volume_MA(20) x 1.0x. Added
                     direction-dispatch context paragraph.
                     Closes GD-7 HIGH (missing short-side
                     EMA-cross inversion).

                     CHANGE 3 — §6 STRATEGY PARAMETER
                     SUMMARY full restructure to mirror
                     TB00000 v2_50 §9.17 canonical
                     registry layout. SSS Signal
                     Parameters split into Long-side
                     (rsi_long_low=30 + rsi_long_high=50),
                     Short-side (rsi_short_low=70 +
                     rsi_short_high=50 + leverage_cap_
                     short=3x + min_order_size_usd=Kraken
                     minimum), and Common (sss_ema_short=9
                     + sss_ema_long=21 + volume_sss_
                     threshold=1.0x). Added Liquidity
                     cache subsection (liquidity_refresh_
                     hours = 4 hours starting; universal-
                     scope annotation). Closes GD-2 HIGH
                     (missing parallel-module framework
                     reflection) + GD-10 MEDIUM (missing
                     liquidity_refresh_hours) + GD-11 HIGH
                     (stale rsi_entry_low/high parameter
                     names renamed to rsi_long_low/high +
                     rsi_short_low/high per 0500000 dv1_80
                     line 6117 Path 2 rename).

                     CHANGE 4 — §10 OPEN ITEMS citation
                     dv1_18 -> dv1_80 (62-version
                     drift). Closes GD-3 MEDIUM.

                     CHANGE 5 — §11 RELATED DOCUMENTS
                     1011003 + 1011012 moved from live
                     "AA=10 implementation" status to
                     "Retired AA=10 coding specs" block
                     with retirement provenance (1011003
                     RETIRED at TB00224; 1011012 RETIRED
                     at TB00227 under DD-012 0500000-
                     canonical-all-docs retirement
                     cascade). Closes GD-4 HIGH (citing
                     retired specs as live implementations).

                     CHANGE 6 — §3 GOVERNING PRINCIPLES
                     new sub-section §3.6 PAPER-AS-MASTER
                     PRINCIPLE REFERENCE pointing to
                     TB00000 §4.4 PA-001..PA-010 paper-
                     as-master principle (enacted v2_18).
                     Closes GD-6 MEDIUM.

                     CHANGE 7 — §3.3 SIGNAL-ALIGNMENT
                     added brief parallel-module dispatch-
                     seam reference per 0500000 dv1_80 D7.
                     Closes GD-8 LOW.

                     CHANGE 8 — §10 OPEN ITEMS "None at
                     document creation" replaced with
                     current-state declaration after
                     housekeeping closure. Closes GD-9
                     LOW.

                     CHANGE 9 — this §12 REVISION HISTORY
                     dv1_2 entry.

                     GD-5 NO ACTION REQUIRED (verification-
                     only entry; §3.4 AR-076 Wilder's SMMA
                     citation matches canonical at 0500000
                     dv1_80).

                     PARENT CHAIN COMPLIANCE.
                     AA=07 Domain Document parent chain
                     preserved: 0700000 IS the Domain
                     Document. Sub-Domain Anchor 0710000
                     dv1_1 + Instance Documents 0711001 +
                     0711002 + 0711003 preserved unchanged
                     (AA=07 sub-campaign next-document
                     audit candidates per TB00215 §8.3
                     carry-forward + TB00321 NSI §1.2(a)).

                     FP/DP ANALYSIS — RR-SCOPE-005
                     DERIVABLE-DECISION EXECUTION.
                     dv1_1 -> dv1_2 amendment execution
                     is a Bill-ratified governance
                     deliverable per DEC-024 (TB00153 DR
                     v1_17) + DEC-024 SUB-DECISION Option
                     B separate-session sequencing
                     (TB00320 chat-turn 1 RR-SCOPE-005 +
                     DD-011 v2_38 OUTCOME 1 single
                     compliant option DERIVABLE). Each
                     surgical edit executed under
                     RR-SCOPE-005 + DD-011 v2_38 PRE-EMIT
                     SCAN compliance. No Bill chat-turn
                     ratification question surfaced.

                     SOURCE-OF-TRUTH PROVENANCE (CI-001).
                     CHANGE 1 + CHANGE 3 (RSI parameters)
                     trace to 0500000 dv1_80 lines 5435-
                     5436 + 5463-5464 + 6117 (Path 2
                     rename) + TB00000 v2_50 §9.17 Long-
                     side SSS signal model + Short-side
                     specific. CHANGE 2 (EMA cross
                     directions) traces to 0500000 dv1_80
                     D7 Dispatch Seam + Sub-Domain 1
                     signal model. CHANGE 3 (parameter
                     registry layout) traces to TB00000
                     v2_50 §9.17 CIATS-OWNED PER-MODULE
                     INSTANTIATED block. CHANGE 4
                     traces to /mnt/project/0500000_
                     dv1_80.html current authoritative
                     version. CHANGE 5 traces to 1010000
                     dv1_19 §4 retirement registry +
                     TB00224 SL + TB00227 SL. CHANGE 6
                     traces to TB00000 v2_50 §4.4
                     PA-001..PA-010 enacted v2_18.
                     CHANGE 7 traces to 0500000 dv1_80
                     D7. CHANGE 8 traces to GD-1..GD-11
                     closure record this amendment.

                     COMPANION DELIVERABLES THIS SESSION
                     (TB00321 SESSION-CLOSE FOUR-
                     OPERATION SEQUENTIAL):
                       0700000 Strategy_dv1_2.txt (this
                         document — nine-CHANGE-block
                         amendment; commits to TothBot_
                         V2-Docs/07XXXXX_Strategy/ per
                         §11 STORAGE + v2_48 §4.5
                         DD-013 REPO-ROUTING rule (ii)).
                       TB00153 MDR v1_37 -> v1_38.txt
                         (DR-068 status update PARTIAL-
                         CLOSED -> FULL-CLOSED; commits
                         to TothBot_V2-SessionLogs per
                         v2_48 DD-013 rule (i)).
                       TB00153 DR v1_17 -> v1_18.txt
                         (DEC-024 SUB-DECISION closure
                         annotation — Option B separate-
                         session EXECUTED at TB00321;
                         commits to TothBot_V2-
                         SessionLogs per v2_48 DD-013
                         rule (i)).
                       TB00321 Session_Log v1_0.txt
                         (OPERATION 3 trio member 1;
                         commits to TothBot_V2-
                         SessionLogs).
                       TB00322 Next_Session_Instructions
                         v1_0.txt (OPERATION 3 trio
                         member 2; commits to TothBot_
                         V2-SessionLogs).

                     OPEN ITEMS UPDATED.
                     GD-1 through GD-11 all CLOSED at
                       document layer via this dv1_2
                       amendment. DR-068 PARTIAL-CLOSED
                       -> FULL-CLOSED via companion MDR
                       v1_38 status update. DEC-024
                       SUB-DECISION Option B EXECUTED
                       via this amendment authoring +
                       MDR + DR closure annotations.

                     Bill override: 0700000 dv1_1 ->
                     dv1_2 is enacted as a derivable
                     execution under DEC-024
                     ratification (TB00153 DR v1_17) +
                     RR-SCOPE-005 derivable-decision
                     authority + DD-011 v2_38 PRE-EMIT
                     SCAN compliance. Direct increment
                     dv1_1 -> dv1_2; no rv state per
                     standing Bill document-control
                     override scope (continuing the
                     scope established TB00115 onward).

  dv1_1  04-25-2026  TB00136 D-03 + D-08 CASCADE CLOSURE.
                     Surgical update closing missed cascade
                     to AA=07 Domain Document. Three content
                     edits + revhist entry.

                     ROOT CAUSE: TB00131 §4.3 5-phase plan
                     cascaded D-03 (universe = USD + USDC +
                     USDT) and D-08 (Top-N cap retired) into
                     0711003 Asset_Selection_Specification
                     dv1_3 (AA=07 Instance Document). The
                     parent Sub-Domain Anchor (0710000) and
                     grandparent Domain Document (this
                     document, 0700000) were never updated.
                     Inverted-cascade defect: descendants
                     correct while ancestors stale. Same
                     root-cause pattern as TB00135 finding
                     (AA=05 → AA=10 missed cascade for WS
                     Manager pathway), but UP one tier in
                     the document hierarchy.

                     DISCOVERY: TB00136 turn 3 audit. Bill
                     asked "Are there any more specification
                     documents that need to be updated?"
                     Comprehensive corpus-wide grep for
                     USDC-without-USDT and Top-N references
                     identified three documents with real
                     gaps: 0700000 (this document), 0710000
                     (Sub-Domain Anchor), 0311002 (governance
                     stale OI status text). Closure executed
                     same-session per RR-SCOPE-005 carry-
                     forward directive.

                     CHANGE 1 — §4.1 ASSET UNIVERSE LAYER:
                     Quote currency criterion (1) updated
                     from "USD or USDC" to "USD, USDC, or
                     USDT" with citation to TB00128 D-03
                     LOCKED.
                     "Starting universe size: top 50 by 24h
                     USD volume" replaced with "Starting
                     universe: ALL Kraken USD/USDC/USDT
                     pairs that satisfy criteria (1)-(3).
                     NO Top-N cap (per TB00128 Ratified
                     Decision D-08 LOCKED — top-N cap
                     retired)."

                     CHANGE 2 — §6 STRATEGY PARAMETERS:
                     Asset Universe Parameters
                     monitored_universe_n value changed
                     from "50 (top N by 24h volume)" to
                     "RETIRED per TB00128 D-08 LOCKED (no
                     top-N cap; full universe)".

                     CHANGE 3 — §7 0711003 DESCRIPTION:
                     "Covers selection criteria (USD/USDC
                     quote, online status, volume threshold),
                     universe size (top 50)" replaced with
                     "Covers selection criteria (USD/USDC/
                     USDT quote, online status, volume
                     threshold), full-universe inclusion
                     (no top-N cap per D-08 LOCKED)".

                     COMPANION DELIVERABLES THIS SESSION:
                     0710000 dv1_0 → dv1_1 (Sub-Domain
                     Anchor cascade closure — partner
                     document; same root cause).
                     0311002 v1_3 → v1_4 (governance OI
                     status text correction — same root
                     cause's downstream documentation).
                     TB00136 Session Log v1_0 → v1_1
                     (extended scope record).
                     TB00136 NSI v1_0 → v1_1 (TB00137 scope
                     update — finding closed; gap removed
                     from forward-scope).

                     OPEN ITEMS UPDATED:
                     None for this document. Cascade gap
                     closed. AA=07 Domain → Sub-Domain →
                     Instance chain now consistent under
                     D-03 + D-08 LOCKED decisions.

  dv1_0  04-05-2026  Initial document. Strategy Domain
                     Document for TothBot V2. Defines
                     domain scope, governing principles
                     (5), hard rules (HR-STRAT-001 through
                     HR-STRAT-010), strategy architecture
                     summary (3 layers: asset universe,
                     regime classification, SSS signal),
                     strategy parameter summary (12 CIATS-
                     owned parameters), relationship to
                     8-gate trading pipeline, document
                     structure (one sub-domain, three
                     instance documents), and domain
                     relationships. All content derived
                     from FP/DP validated 0500000 dv1_18
                     AR-067 through AR-076.

============================================================
END OF DOCUMENT
============================================================
