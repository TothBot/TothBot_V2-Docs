DocDCN:     1011012
DocTitle:   Regime_Engine_Coding_Spec
DocVersion: dv1_5
DocOwner:   Bill
DocPath:    GitHub.com/TothBot/TothBot_V2-Docs/10XXXXX_Coding_Specification/1011012_Regime_Engine_Coding_Spec_dv1_5.txt
DocDate:    04-24-2026
DocTime:    23:59:59 UTC

============================================================
REGIME ENGINE CODING SPECIFICATION
============================================================

============================================================
1. PURPOSE
============================================================

This document is the complete, unambiguous coding contract
for the TothBot V2 Regime Engine. It classifies the market
regime for each pair daily at 00:00 UTC. Regime classification
is the highest-priority correctness requirement in the system.
An incorrect regime classification directly causes loss.

============================================================
2. PARENT CHAIN
============================================================

  1000000  Coding_Specification
  1010000  Trading_System_Coding_Anchor
  1011012  This document

  Primary parent specification:
    0711002  Regime_Taxonomy_Specification  dv1_0
             (6-regime taxonomy, ADX/EMA/ATR thresholds,
             Gate 3 and Gate 6 mapping)

  Secondary parent specification (BTC/USD market_regime
  proxy and universe scope):
    0711003  Asset_Selection_Specification  dv1_3
             (AS-003 BTC/USD regime-anchor + CIATS-owned
             pipeline evaluation rule; HR-AS-001 BTC/USD
             always subscribed as regime anchor; HR-AS-009
             BTC/USD pipeline evaluation CIATS-owned /
             default-disabled)

  Architectural authority:
    0500000  System_Architecture_Overview  dv1_20
             AR-070 Asset selection universe (scope amended
             TB00130 Ratified Decision D-03: quote currency
             USD + USDC + USDT, no top-N cap).
             AR-074 BTC/USD always in monitored universe —
             market_regime proxy commitment (UNCHANGED
             dv1_20). AR-074 assigns RE-MKT-001 through
             RE-MKT-003 to 1011012.

============================================================
3. FIVE CODING-READINESS QUESTIONS
============================================================

(1) What does Regime Engine DO?
    Classifies the current market regime for each pair
    in the trading universe daily at 00:00 UTC using
    committed daily OHLC candles from Kraken REST API.
    Staggered 1.1s between pairs (rate limit compliance).
    Stores result in regime_cache. Provides stale fallback
    on failure. Two-level tagging: asset regime + BTC/USD
    market regime. Gate 3 and Gate 6 read this cache.
    BTC/USD is ALWAYS in the computation set as the
    market_regime proxy per §10 RE-MKT-001 / AR-074,
    regardless of BTC/USD's pipeline-evaluation state
    (CIATS-owned default-disabled per 0711003 dv1_3
    HR-AS-009).

(2) Interfaces?
    Input: Kraken REST GetOHLCData (interval=1440, interval=5)
           tradeable_universe handle from WS Manager
             (populated per 0711003 dv1_3 AS-004 / AS-006;
             BTC/USD guaranteed member per §10 RE-MKT-001
             and 0511001 dv1_3 HR-WM-027)
    Output: regime_cache dict[str, RegimeState]
            market_regime (BTC/USD) tagged at entry
    Consumers: Signal Pipeline Gate 3 (pre-filter),
               Gate 6 (sizing modifier),
               Position Mirror (regime tag on trade entry),
               CIATS (per-regime P&L archiving)

(3) Hard rules?
    ALWAYS exclude response[-1] (uncommitted candle).
    ALWAYS use committed_candles = candles[:-1].
    ALWAYS stagger 1.1s between pair REST calls.
    NEVER use asyncio.gather() for regime OHLC calls.
    NEVER halt on regime computation failure — use stale.
    ADX batch computation on full history (all committed).
    NEVER compute ADX incrementally in Regime Engine.
    BTC/USD ALWAYS in trading universe (market regime proxy).
    BTC/USD regime ALWAYS computed (§10 RE-MKT-002) even
      when its pipeline evaluation is CIATS-owned
      default-disabled.
    regime_cache updated atomically per pair.
    Stale regime: use prior day, log REGIME_STALE_WARNING.

(4) What triggers it?
    00:00 UTC daily — asyncio.Task scheduled at midnight.
    Also at startup (Step 8 Call C per 1011014 SS-WARMUP-004).

(5) What does it log?
    REGIME_COMPUTED: market_regime, asset_regime per pair,
      adx_14, ema_20, ema_50, atr_pct, computed_at.
    REGIME_STALE_WARNING: hours_since_update, pair.
    WARMUP_REST_FAILED: pair, interval, error.
    REGIME_RATE_LIMIT_HIT: pair, retry_count.

============================================================
4. REGIME TAXONOMY
============================================================

Six regimes from 0711002 Regime_Taxonomy_Specification.
All thresholds are CIATS-owned starting values.

4.1 Directional Regime (from ADX and EMA):

  TRENDING_POSITIVE:  ADX(14) > 25 AND EMA(20) > EMA(50)
  TRENDING_NEGATIVE:  ADX(14) > 25 AND EMA(20) < EMA(50)
  NON_DIRECTIONAL:    ADX(14) <= 25

  adx_threshold:     25  (CIATS-owned starting value)
  htf_ema_periods:   20 / 50  (CIATS-owned starting value)
  window:            50 days rolling

4.2 Volatility Regime (from ATR percentile):

  NORMAL_VOL:         ATR percentile <= 67th
  ELEVATED_VOL:       ATR percentile > 67th

  atr_percentile_thresh: 67th percentile (CIATS-owned)
  window:             trailing 50 daily ATR values

4.3 Complete 6-regime cross:

  TRENDING_POS + NORMAL_VOL     ← ENTRY ALLOWED
  TRENDING_POS + ELEVATED_VOL   ← ENTRY ALLOWED (50% size)
  NON_DIR + NORMAL_VOL          ← NO ENTRY
  NON_DIR + ELEVATED_VOL        ← NO ENTRY
  TRENDING_NEG + NORMAL_VOL     ← NO ENTRY
  TRENDING_NEG + ELEVATED_VOL   ← NO ENTRY

  Gate 3 blocks: TRENDING_NEG (any vol), NON_DIR + ELEVATED.
  Gate 6 sizes at 50% for NON_DIR + NORMAL (no, blocked by Gate 3).
  Gate 6 sizes at 50% for TRENDING_POS + ELEVATED (allowed entry,
    reduced size due to elevated volatility risk).

============================================================
5. DATA SOURCING — GETOHCLDATA REQUIREMENTS
============================================================

RE-OHLC-001  CRITICAL: The LAST candle in any GetOHLCData
             response is ALWAYS the current not-yet-committed
             candle. Its data is incomplete (mid-candle).
             ALWAYS exclude it.

RE-OHLC-002  MANDATORY pattern for all OHLC data usage:
               data = await get_ohlc_data(pair, interval=1440)
               committed_candles = data[:-1]  # ALWAYS exclude last
             NO EXCEPTIONS. Using data[-1] in any computation
             corrupts all indicators: ADX (zero DM), EMA (biased
             close), ATR (zero range). This is a silent error —
             the computation runs but produces wrong values.

RE-OHLC-003  Available daily candles (Kraken API returns 720):
               response length: 720 candles
               committed_candles = response[:-1] = 719 candles
               This is confirmed available for all pairs
               in the Kraken trading universe.

RE-OHLC-004  Decimal conversion immediately on parse:
               for candle in committed_candles:
                   open_d  = Decimal(str(candle["open"]))
                   high_d  = Decimal(str(candle["high"]))
                   low_d   = Decimal(str(candle["low"]))
                   close_d = Decimal(str(candle["close"]))
                   volume_d = Decimal(str(candle["volume"]))
             Per BP-DEC-001: all OHLC values converted to
             Decimal via string constructor immediately.

============================================================
6. INDICATOR COMPUTATION
============================================================

------------------------------------------------------------
6.1 ATR(14) — Wilder's True Range (daily)
------------------------------------------------------------

RE-IND-001  ATR computation uses committed_candles only.
            This is the DAILY ATR used for VOLATILITY REGIME.
            It is a different value from the 5-min ATR used
            for MAE threshold in the pipeline.

RE-IND-002  True Range computation:
              prev_close = committed_candles[0].close
              for i, candle in enumerate(committed_candles[1:]):
                  TR = max(
                      candle.high - candle.low,
                      abs(candle.high - prev_close),
                      abs(candle.low  - prev_close)
                  )
                  tr_list.append(TR)
                  prev_close = candle.close

RE-IND-003  ATR(14) — Initial + Wilder smoothing:
              if len(tr_list) < 14:
                  # Insufficient data — use stale or skip
                  return None
              # Simple average for first 14 values
              atr = sum(tr_list[:14]) / 14
              # Wilder smoothing forward
              for tr in tr_list[14:]:
                  atr = (atr * 13 + tr) / 14
              # All operations in Decimal

RE-IND-004  ATR percentile (trailing 50 daily ATR values):
              from statistics import quantiles
              atr_window = [daily_atr[i] for i in -50:]
              p67 = quantiles(atr_window, n=100)[66]
              vol_regime = (ELEVATED_VOL if daily_atr[-1] > p67
                            else NORMAL_VOL)
              Minimum window: 30 values for stability.

------------------------------------------------------------
6.2 ADX(14) — Wilder's batch computation
------------------------------------------------------------

RE-IND-005  ADX computation uses committed_candles only.
            Batch Wilder smoothing on full 719-candle history
            daily. NEVER incremental in Regime Engine.

RE-IND-006  Full Wilder batch ADX computation:
              # See 0711002 RE-013 for complete pseudocode.
              # Key points:
              #   - +DM, -DM computed per candle
              #   - TR computed per candle (same as ATR)
              #   - First 14: simple sums (not averages)
              #   - Forward: Wilder smoothing
              #       smoothed = prev_smoothed - (prev_smoothed/14) + current
              #   - +DI = 100 * (smoothed_+DM / smoothed_TR)
              #   - -DI = 100 * (smoothed_-DM / smoothed_TR)
              #   - DX  = 100 * abs(+DI - -DI) / (+DI + -DI)
              #   - ADX = Wilder-smoothed DX (14-period)
              # All operations in Decimal throughout.

------------------------------------------------------------
6.3 EMA(20) and EMA(50) — batch exponential moving average
------------------------------------------------------------

RE-IND-007  Batch EMA computation on committed_candles:
              def compute_ema(closes, period):
                  if len(closes) < period:
                      return None
                  alpha = Decimal(2) / Decimal(period + 1)
                  ema = sum(closes[:period]) / period
                  for close in closes[period:]:
                      ema = alpha * close + (1 - alpha) * ema
                  return ema

              closes = [candle.close for candle in committed_candles]
              ema_20 = compute_ema(closes, 20)
              ema_50 = compute_ema(closes, 50)

RE-IND-008  Directional regime classification:
              if adx_14 > adx_threshold:  # 25 CIATS-owned
                  if ema_20 > ema_50:
                      directional = TRENDING_POSITIVE
                  else:
                      directional = TRENDING_NEGATIVE
              else:
                  directional = NON_DIRECTIONAL

============================================================
7. SCHEDULING AND RATE LIMITING
============================================================

RE-SCH-001  Runs once daily at 00:00 UTC for ALL pairs.
            Implemented as asyncio.Task with asyncio.sleep
            until next midnight. Persistent task.

RE-SCH-002  Must complete before first 5m candle close
            of the new day (~00:05 UTC worst case).
            With 50 pairs x 1.1s stagger = 55 seconds.
            Completion by ~00:01 UTC. No risk of race.
            Note: Universe size under D-03 amended (USD +
            USDC + USDT, no top-N cap) is bounded by the
            $500k daily volume filter (0711003 dv1_3 AS-001
            Criterion 3). Startup-measured universe size
            is the planning input for this window; CIATS
            EWMA Monitor tracks regime_computation_elapsed_s
            for capacity-planning signal.

RE-SCH-003  On regime computation failure: use stale
            regime from prior computation. Log
            REGIME_STALE_WARNING. Alert if stale > 24h.
            Never halt TothBot on regime failure.

RE-RL-001   CRITICAL: Kraken REST GetOHLCData rate limit
            is 1 call/second per pair. MUST stagger.

RE-RL-002   MANDATORY stagger pattern:
              for pair in trading_universe:
                  data = await get_ohlcdata(pair, interval=1440)
                  compute_and_cache_regime(pair, data)
                  await asyncio.sleep(1.1)

RE-RL-003   NEVER use asyncio.gather() for regime OHLC calls.
            asyncio.gather() parallelizes calls — all hit
            Kraken at the same time → rate limit rejection.
            50 pairs x 1.1s = 55s sequential. Acceptable.

RE-RL-004   On HTTP 429: backoff 2s, 4s, 8s. Max 3 retries.
            If all fail: log WARMUP_REST_FAILED. Use stale.

============================================================
8. REGIME CACHE AND TAGGING
============================================================

RE-TAX-001  Six regimes, each is a combination of
            directional + volatility:
              TRENDING_POSITIVE + NORMAL_VOL
              TRENDING_POSITIVE + ELEVATED_VOL
              TRENDING_NEGATIVE + NORMAL_VOL
              TRENDING_NEGATIVE + ELEVATED_VOL
              NON_DIRECTIONAL + NORMAL_VOL
              NON_DIRECTIONAL + ELEVATED_VOL

RE-TAX-002  regime_cache structure:
              from dataclasses import dataclass

              @dataclass
              class RegimeState:
                  directional:  str   # TRENDING_POS/NEG/NON_DIR
                  vol_regime:   str   # NORMAL_VOL/ELEVATED_VOL
                  adx_14:       Decimal
                  ema_20:       Decimal
                  ema_50:       Decimal
                  atr_daily:    Decimal
                  atr_pct_rank: Decimal
                  computed_at:  str   # ISO 8601 UTC

              regime_cache: dict[str, RegimeState] = {}
              # Keys: pair symbols e.g. "BTC/USD", "ETH/USD"

RE-TAX-003  Gate 3 (Regime Pre-Filter) reads regime_cache:
              state = regime_cache.get(symbol)
              if state is None:
                  return BLOCK  # No regime data
              if state.directional == TRENDING_NEGATIVE:
                  return STOP   # No entry
              if (state.directional == NON_DIRECTIONAL
                  and state.vol_regime == ELEVATED_VOL):
                  return STOP   # No entry

RE-TAG-001  TWO regime tags on every trade:
            asset_regime:  regime_cache[symbol]
            market_regime: regime_cache["BTC/USD"]
            Both written to Position Mirror at entry.
            Both written to TRADE_CLOSE Logger record.
            See §10 RE-MKT-003 for the formal architectural
            commitment to two-tag tagging.

RE-TAG-002  BTC/USD ALWAYS in trading universe.
            Computed daily alongside all other pairs.
            market_regime serves as systemic risk proxy.
            See §10 RE-MKT-001 for the formal architectural
            commitment to BTC/USD membership.

RE-STL-001  Stale regime: use prior RegimeState if fresh
            computation fails. Log REGIME_STALE_WARNING.
            computed_at timestamp used to detect staleness.

RE-STL-002  Never halt TothBot on stale regime.
            If stale > 24h: alert operator HIGH.
            If regime_cache[symbol] is None:
              Gate 3 returns STOP (no entry permitted).

RE-STL-003  On successful computation, write:
              regime_cache[symbol] = RegimeState(
                  directional=..., vol_regime=...,
                  adx_14=..., ema_20=..., ema_50=...,
                  atr_daily=..., atr_pct_rank=...,
                  computed_at=datetime.now(timezone.utc).isoformat()
              )
            Log REGIME_COMPUTED with all field values.

============================================================
9. ADX CONVERGENCE — CONFIRMED ADEQUATE
============================================================

RE-FPD-001  ADX(14): 28 candle minimum for first valid value.
            EMA(20): 20 minimum. ATR(14): 14 minimum.
            Overall minimum: 28 committed candles.

RE-FPD-002  Available committed candles: 719 (from 720 returned).
            691+ candles beyond minimum. Data confirmed sufficient.

RE-FPD-003  ATR percentile: 30 candle minimum for stability.
            With 719 candles: confirmed adequate.

RE-FPD-004  Full ADX convergence accuracy: 2xN+100 = 128 candles.
            Available: 719 candles = 5.6x the convergence requirement.
            TothBot uses batch computation on all 719 candles.
            Full convergence guaranteed.

RE-FPD-005  Regime Engine computes ADX in BATCH on the full
            719-candle history. NOT incrementally. Wilder
            smoothing recursion runs on all available data
            every day at 00:00 UTC.

RE-FPD-006  TWO ATR computations in TothBot — DISTINCT:
            (1) Regime Engine: daily ATR from 1440-interval candles
                (batch, 719 candles, for volatility regime)
            (2) Pipeline: 5-min ATR from incremental candle events
                (running, 14-candle warm-up, for MAE/emergSL)
            These are different computations for different purposes.
            Never confuse them. Single source of truth per type.

============================================================
10. BTC/USD MARKET REGIME — ARCHITECTURAL COMMITMENT
============================================================

This section fulfills the 0500000 dv1_20 AR-074 assignment
of RE-MKT-001 through RE-MKT-003 to 1011012. AR-074 is
UNCHANGED in 0500000 dv1_20 — the BTC/USD market_regime
proxy commitment was not modified by TB00130 D-03
amendment. D-03 (universe scope amended to USD + USDC +
USDT, no top-N cap) DID place BTC/USD into
tradeable_universe under 0711003 dv1_3 AS-001 Criterion 1
(previously excluded by the "USDC or USDT" criterion). The
revalidation this session confirms that AR-074 semantics
hold under D-03 amended: BTC/USD's regime-anchor role and
its CIATS-owned default-disabled pipeline evaluation
coexist cleanly with its new universe-member status.

RE-MKT-001  BTC/USD REGIME COMPUTATION — ALWAYS.
            BTC/USD is ALWAYS in the Regime Engine
            computation set. Its inclusion does NOT depend
            on the tradeable_universe size, ranking, volume
            filter, or any CIATS universe-management
            decision. Per 0500000 dv1_20 AR-074 and 0711003
            dv1_3 HR-AS-001, BTC/USD is the market_regime
            proxy for TothBot V2 and must have a fresh
            regime computation every daily cycle.
            Enforcement:
              (a) Startup: the warm-up routine (Step 8
                  Call C per 1011014 SS-WARMUP-004) MUST
                  compute BTC/USD regime before any other
                  pair. If BTC/USD regime computation fails
                  at startup, the system transitions to
                  HALTED — BTC/USD regime is a precondition
                  for any entry.
              (b) Daily 00:00 UTC cycle: BTC/USD regime
                  computation is issued FIRST in the
                  stagger sequence (RE-RL-002). If it
                  fails, the system falls back to stale
                  BTC/USD regime per RE-STL-001, logs
                  REGIME_STALE_WARNING with
                  pair="BTC/USD", and raises HIGH alert
                  if stale > 24h per RE-STL-002.
              (c) Universe membership: under D-03 amended
                  (0711003 dv1_3 AS-001 Criterion 1), USD
                  quote is in scope, so BTC/USD IS a member
                  of tradeable_universe. However, BTC/USD's
                  presence in tradeable_universe does NOT
                  create the regime-computation commitment
                  — the commitment exists independently per
                  AR-074 and HR-AS-001, and it predates the
                  D-03 amendment. Even in a hypothetical
                  future where D-03 is reversed, BTC/USD
                  regime computation remains mandatory.
            Per 0711003 dv1_3 AS-003 (BTC/USD regime anchor)
            and 0511001 dv1_3 HR-WM-027 (regime_anchor
            subscription is permanent).

RE-MKT-002  BTC/USD PIPELINE EVALUATION — CIATS-OWNED /
            DEFAULT-DISABLED.
            BTC/USD pipeline evaluation for ENTRY is
            CIATS-OWNED and DISABLED BY DEFAULT per 0500000
            dv1_20 AR-074 and 0711003 dv1_3 HR-AS-009.
              - Default state: Signal Pipeline Gate 1
                (State Machine pre-filter) excludes BTC/USD
                from the entry-evaluation set. BTC/USD
                signals are NOT generated; Selection
                Controller is NOT invoked for BTC/USD under
                default CIATS state. See 1011009 dv1_1
                SC-UNI-003 for Selection Controller
                consequence.
              - CIATS-enabled state: after 200 closed-trade
                statistical-inference floor (per TB00000
                §7 and 0211001 CIATS_Specification PDCA
                procedure), CIATS MAY enable BTC/USD
                pipeline evaluation via Parameter Store
                update. Once enabled, BTC/USD is treated
                as any other tradeable_universe member —
                signals are generated, Selection Controller
                applies all 6 gates with no special BTC/USD
                handling.
            Regime Engine behavior is INVARIANT across the
            two states:
              - Default (disabled): BTC/USD regime computed
                every cycle per RE-MKT-001. Not consumed by
                Signal Pipeline entry path (Gate 1 excludes
                before SSS engine runs). Consumed by every
                other pair's market_regime tag per
                RE-MKT-003.
              - CIATS-enabled: BTC/USD regime computed
                every cycle per RE-MKT-001 (identical).
                Consumed by BTC/USD's own entry path AND
                as every pair's market_regime tag per
                RE-MKT-003.
            Regime Engine does NOT read the CIATS BTC/USD-
            enabled parameter. It computes unconditionally.
            The enabled/disabled state is a concern of
            Signal Pipeline Gate 1 only.

RE-MKT-003  TWO REGIME TAGS PER TRADE — INVARIANT.
            Every position opened by TothBot V2 carries TWO
            regime tags, written at entry to Position Mirror
            and at close to the Logger TRADE_CLOSE record.
            This tagging is MANDATORY and UNCONDITIONAL:
              asset_regime:   regime_cache[symbol]
                              (the traded pair's own regime
                              at entry)
              market_regime:  regime_cache["BTC/USD"]
                              (BTC/USD regime at entry —
                              systemic risk proxy)
            Defect conditions:
              - asset_regime is None at entry →
                  Position Mirror REJECTS the entry write
                  (Signal Pipeline Gate 3 should have
                  blocked upstream; if this path is reached,
                  Gate 3 has a defect).
              - market_regime is None at entry →
                  HALT the system. BTC/USD regime missing
                  contradicts RE-MKT-001. Alert HIGH.
            Both tags MUST be present on every TRADE_CLOSE
            record for CIATS regime-segmented P&L analysis
            (per 0211001 CIATS_Specification and 0500000
            A-1 signal set). A TRADE_CLOSE record missing
            either tag is a CIATS corpus defect and
            triggers a CIATS data-quality alert per AR-007
            pattern.
            This invariant holds in BOTH paper mode and
            live mode per 0711003 dv1_3 HR-AS-010 (paper-
            to-live parity EXACT per TB00128 D-06).

============================================================
11. REVISION HISTORY
============================================================

  dv1_5  04-24-2026  AR-074 RE-MKT-* assignment fulfilled.
                     Phase 2 Step 4 of TB00131 5-phase spec
                     cascade plan executed.

                     PRIMARY CHANGE — §10 BTC/USD MARKET
                     REGIME ARCHITECTURAL COMMITMENT ADDED:
                     New Section 10 adds RE-MKT-001 through
                     RE-MKT-003 rules formalizing BTC/USD's
                     role as market_regime proxy. These
                     rule IDs were ASSIGNED by 0500000
                     dv1_20 AR-074 but were absent from the
                     coding spec (latent governance gap
                     parallel to 1011009 SC-UNI-*). dv1_5
                     closes the gap.

                       RE-MKT-001  BTC/USD regime ALWAYS
                                   computed every cycle,
                                   independent of
                                   tradeable_universe
                                   membership. Startup and
                                   daily-cycle enforcement
                                   points defined.
                       RE-MKT-002  BTC/USD pipeline
                                   evaluation is CIATS-
                                   owned / default-disabled
                                   per AR-074 / HR-AS-009.
                                   Regime Engine behavior
                                   INVARIANT across
                                   disabled/enabled states
                                   (computes
                                   unconditionally). CIATS
                                   BTC/USD-enabled flag is
                                   read by Signal Pipeline
                                   Gate 1 ONLY.
                       RE-MKT-003  Two regime tags per
                                   trade (asset_regime +
                                   market_regime) INVARIANT
                                   in paper and live per
                                   HR-AS-010 / D-06.
                                   Position Mirror rejects
                                   entry on missing
                                   asset_regime. System
                                   HALTs on missing
                                   market_regime. CIATS
                                   corpus integrity defect
                                   on missing tag in
                                   TRADE_CLOSE.

                     REVALIDATION UNDER D-03 AMENDED:
                     Per TB00131 NSI §4.1, 1011012 dv1_5
                     was expected to be a revalidation-only
                     edit. Revalidation result:
                       (a) AR-074 text UNCHANGED in 0500000
                           dv1_20. Semantics preserved.
                       (b) BTC/USD is NOW a member of
                           tradeable_universe under D-03
                           amended (0711003 dv1_3 AS-001
                           Criterion 1 admits USD quote).
                           This is a STATE CHANGE upstream;
                           AR-074 was not affected because
                           AR-074 defines regime-anchor
                           commitment INDEPENDENTLY of
                           universe membership. BTC/USD's
                           dual role (tradeable_universe
                           member + regime_anchor) is
                           formalized in RE-MKT-001
                           paragraph (c).
                       (c) BTC/USD pipeline-evaluation
                           default-disabled behavior
                           (HR-AS-009 / AR-074) is
                           formalized in RE-MKT-002.
                           Regime Engine was previously
                           silent on this state machine;
                           dv1_5 documents that Regime
                           Engine's behavior is INVARIANT
                           across the state — it computes
                           unconditionally. The
                           enabled/disabled state is read
                           only by Signal Pipeline Gate 1.
                     Revalidation produced content
                     additions (new §10) rather than
                     content modifications. Existing rules
                     RE-OHLC-*, RE-IND-*, RE-SCH-*, RE-RL-*,
                     RE-TAX-*, RE-TAG-*, RE-STL-*, RE-FPD-*
                     are UNCHANGED.

                     CHANGE 2 — PARENT CHAIN EXTENDED:
                     Section 2 extended with secondary
                     parent citation for 0711003
                     Asset_Selection_Specification dv1_3
                     (BTC/USD regime-anchor + CIATS-owned
                     pipeline rule). Architectural
                     authority block cites 0500000 dv1_20
                     AR-070 (context: universe scope
                     amended) and AR-074 (RE-MKT-* source).

                     CHANGE 3 — §3 Q(1) / Q(2) / Q(3) /
                     Q(4) UPDATED:
                     Q(1) adds sentence clarifying BTC/USD
                     ALWAYS in computation set per
                     RE-MKT-001 and HR-AS-009 default-
                     disabled pipeline state. Q(2) adds
                     tradeable_universe handle as input.
                     Q(3) adds BTC/USD regime ALWAYS
                     computed hard rule. Q(4) updates
                     stagger sequence note to reflect
                     BTC/USD-first ordering per
                     RE-MKT-001(b) — also reflected in
                     §7 RE-SCH-002 narrative.

                     CHANGE 4 — §7 RE-SCH-002 CLARIFIED:
                     RE-SCH-002 narrative updated to note
                     that universe size under D-03 amended
                     is bounded by the $500k volume filter
                     (0711003 dv1_3 AS-001 Criterion 3)
                     rather than by a retired top-N cap.
                     CIATS EWMA Monitor signal
                     regime_computation_elapsed_s cited
                     as capacity-planning signal.

                     CHANGE 5 — §8 RE-TAG-001 / RE-TAG-002
                     CROSS-REFERENCE:
                     RE-TAG-001 adds "See §10 RE-MKT-003"
                     pointer. RE-TAG-002 adds "See §10
                     RE-MKT-001" pointer. Existing
                     behavior text preserved verbatim; §10
                     provides the formal architectural
                     commitment that §8 previously
                     documented only as implementation.

                     SECTIONS UNCHANGED:
                     §1 Purpose. §4 Regime Taxonomy. §5
                     Data Sourcing (RE-OHLC-001..004). §6
                     Indicator Computation (RE-IND-001..008).
                     §9 ADX Convergence (RE-FPD-001..006).
                     Rule text for RE-OHLC-*, RE-IND-*,
                     RE-SCH-001/003, RE-RL-*, RE-TAX-*,
                     RE-TAG-* body, RE-STL-*, RE-FPD-*
                     all preserved verbatim.

                     FIVE CODING-READINESS QUESTIONS —
                     ALL FIVE ANSWERED:
                     (1) DO — classifies market regime for
                     each universe pair daily; BTC/USD
                     always in set. (2) INTERFACES — Kraken
                     REST + universe handle in; regime_cache
                     + market_regime tag out; Gate 3 + Gate
                     6 + Position Mirror + CIATS consumers.
                     (3) HARD RULES — exclude response[-1],
                     stagger 1.1s, no gather(), no halt on
                     failure, BTC/USD always computed.
                     (4) TRIGGERS — 00:00 UTC daily;
                     startup warm-up. (5) LOGS —
                     REGIME_COMPUTED, REGIME_STALE_WARNING,
                     WARMUP_REST_FAILED,
                     REGIME_RATE_LIMIT_HIT. Document
                     remains coding-ready.

                     FP/DP ANALYSIS (RE-MKT-* content):
                     Options considered:
                       Option A — Leave §8 RE-TAG-001 /
                         RE-TAG-002 as the sole location
                         for BTC/USD market_regime
                         architectural language.
                         REJECTED — AR-074 explicitly
                         assigns RE-MKT-001..003 to 1011012
                         (0500000 dv1_20 line 1214).
                         Leaving the assignment unfulfilled
                         is a governance gap.
                       Option B — Rewrite §8 to absorb
                         RE-MKT-* into existing RE-TAG-*
                         and RE-TAX-*. REJECTED —
                         rewriting reduction in existing
                         text creates regression risk for
                         the ADX convergence and Wilder
                         batch computation sections that
                         depend on §8 structural stability.
                         Also violates minimum-scope edit
                         principle.
                       Option C — Add new Section 10
                         formalizing RE-MKT-* as an
                         architectural-commitment section,
                         with §8 narrative cross-references
                         added as single-line pointers.
                         ACCEPTED — fulfills AR-074
                         assignment, preserves §8 body
                         text verbatim, adds minimum-scope
                         cross-reference pointers. Three
                         rules (001/002/003) map cleanly
                         to the three distinct concerns:
                         computation invariance, pipeline
                         evaluation state, tagging
                         invariance.
                     Decision: Option C.

                     BILL OVERRIDE:
                     Document-control constraints for
                     1011012 (dv1_4 → dv1_5 minor increment
                     via repair cascade, no rv state) are
                     governed by Bill's standing directive
                     continuing the override scope
                     established TB00115 (04-22-2026) and
                     extended through TB00116, TB00128,
                     TB00129, TB00130, TB00131. Phase 2 of
                     the TB00131 §4.3 5-phase plan executes
                     under this override in TB00132.

                     No patches. No real money. Paper
                     trading must mimic all aspects of live
                     trading exactly. Document is coding-
                     ready.

  dv1_4  04-05-2026  Phase 8 completion. WD → full spec.
                     Parent chain updated (1000000, 1010000,
                     0711002 now exist). WD status removed.
                     Sections 3.1-3.8 from dv1_1 (key rules
                     summary) absorbed into full Sections 4-9.
                     Section 4: Full 6-regime taxonomy from
                     0711002 with Gate 3/6 mapping. Section 5:
                     Data sourcing rules with Decimal conversion
                     code. Section 6: Complete indicator
                     computation code (ATR, ADX, EMA) with
                     full Wilder batch implementation.
                     Section 7: Scheduling and rate limiting.
                     Section 8: Regime cache dataclass and
                     Gate 3 read pattern. Section 9: ADX
                     convergence confirmation carries forward
                     from dv1_3. Five coding-readiness
                     questions answered (Section 3).
                     Document is coding-ready.

  dv1_3  04-04-2026  DocPath correction.

  dv1_2  03-25-2026  ADX convergence validation (128 candles,
                     5.6x factor of safety confirmed).

  dv1_1  03-25-2026  Rate limit stagger. BTC/USD tagging.
                     Stale fallback.

  dv1_0  03-24-2026  Initial WD placeholder.

============================================================
END OF DOCUMENT
============================================================
