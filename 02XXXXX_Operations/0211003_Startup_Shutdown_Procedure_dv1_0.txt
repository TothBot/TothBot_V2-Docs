DocDCN:     0211003
DocTitle:   Startup_Shutdown_Procedure
DocVersion: dv1_0
DocOwner:   Bill
DocPath:    GitHub.com/TothBot/TothBot_V2-Docs/02XXXXX_Operations/0211003_Startup_Shutdown_Procedure_dv1_0.txt
DocDate:    04-05-2026
DocTime:    23:59:59 UTC

============================================================
STARTUP SHUTDOWN PROCEDURE
============================================================

============================================================
1. PURPOSE
============================================================

This document defines the complete, deterministic startup
and shutdown sequence for TothBot V2. Every step is
mandatory. No step may be skipped. No pipeline evaluation
may occur until the startup sequence is complete and at
least one pair reaches READY state.

This document governs WHAT TothBot V2 does on startup
and shutdown and WHY. Implementation details are in
AA=10 coding specification 1011014 Startup_Sequence_
Coding_Spec.

============================================================
2. PARENT CHAIN
============================================================

  0000000  Ethics_and_Safety              (Domain)
  0100000  Management                     (Domain)
  0200000  Operations                     (Domain Document)
  0210000  Operations_CIATS_Anchor        (Sub-Domain Anchor)
  0211003  This document                  (Instance)

============================================================
3. PAIR STATES
============================================================

Every monitored pair is in exactly one of three states
at all times:

  WARM_UP:  Pair is eligible for trading but caches are
            not yet seeded. Not available for pipeline
            evaluation. Seeding in progress via REST
            GetOHLCData or incrementally via WS candle
            events. Transitions to READY when all three
            required caches are populated: ATR(14),
            1H EMA(20)/EMA(50), and current daily regime.

  READY:    All caches seeded. Pair participates in the
            full 8-gate pipeline evaluation on every
            5-minute candle close. Pipeline evaluation
            begins as soon as at least one pair reaches
            READY state.

  BLOCKED:  Pair instrument status is not online or
            post_only (i.e., reduce_only, work_in_progress,
            limit_only, delisted, or maintenance).
            Pair is excluded from all pipeline evaluation.
            Re-evaluated on each instrument channel update.
            Transitions to WARM_UP if status returns to
            online or post_only.

============================================================
4. STARTUP SEQUENCE (10 STEPS)
============================================================

The startup sequence is governed by AR-049 (0500000
dv1_18). All 10 steps must complete before pipeline
evaluation begins. Steps execute in the order listed.

------------------------------------------------------------
SS-START-001  STEP 1 — Kraken Status API Check
------------------------------------------------------------

Query: https://status.kraken.com/api/v2/scheduled-
       maintenances.json

Check for scheduled maintenance windows. If a maintenance
event is scheduled within 2 hours: log CRITICAL and alert
alert@tothbot.com. This check is informational only — it
does not block startup. Bill decides whether to stand down.

If the Status API is unavailable: log WARNING and continue.
A Status API failure does not halt startup.

------------------------------------------------------------
SS-START-002  STEP 2 — Acquire WebSocket Token
------------------------------------------------------------

Call Kraken REST API: GetWebSocketsToken.
This token authenticates the private WebSocket connection.

The token is valid for 15 minutes. It must be refreshed
before expiry on every reconnection event. Token acquisition
failure is fatal: retry 3 times. If all retries fail,
TothBot cannot establish the authenticated WS connection.
systemd will restart TothBot on watchdog timeout.

------------------------------------------------------------
SS-START-003  STEP 3 — Connect Public WS and Subscribe
------------------------------------------------------------

Establish connection to wss://ws.kraken.com/v2.

Subscribe in a single message:
  - instrument channel (snapshot: true) — pair specs
  - status channel (snapshot: true) — engine state
  - ohlc channel, interval=5 (snapshot: true) — system clock
  - ohlc channel, interval=60 (snapshot: true) — 1H HTF cache

Do NOT subscribe ticker at this step. Ticker subscriptions
are managed per-pair after Position Mirror is restored
(Step 7).

Start the WS ping task (30-second interval) and zombie
detection task (90-second no-real-data threshold) on this
connection immediately after subscription.

------------------------------------------------------------
SS-START-004  STEP 4 — Process Instrument Snapshot
------------------------------------------------------------

Wait for the instrument channel snapshot message.
Timeout: 10 seconds. On timeout: reconnect and retry
from Step 3.

For each pair in the snapshot, populate the per-pair
instrument cache: price_increment, qty_increment, qty_min,
cost_min, status. All numeric values converted to Decimal
immediately on parse.

Set initial pair state from instrument status:
  online or post_only → WARM_UP
  all other statuses  → BLOCKED

------------------------------------------------------------
SS-START-005  STEP 5 — Connect Private WS and Subscribe
------------------------------------------------------------

Establish connection to wss://ws-auth.kraken.com/v2
using the token acquired in Step 2.

Subscribe executions channel with:
  snap_orders: true  — snapshot of all open orders
  snap_trades: true  — last 50 fills for gap reconciliation
  order_status: true — MANDATORY: without this, amended
                       and restated orders are silently
                       dropped. Non-negotiable. (AR-026)
  ratecounter: true  — real-time rate limit monitoring

Extract maxratecount from the executions subscription ACK.
Use this as the operative rate limit threshold.

Subscribe balances channel with snapshot: true.
Parse the balances snapshot: extract spot/main wallet
USD balance only. The top-level balance field is the sum
across all wallet types — it must NOT be used. Only
wallets[type="spot"][id="main"].balance is tradeable
capital (AR-050).

Set portfolio_baseline_USD = spot_usd_balance at this
exact moment. This is the ONLY time portfolio_baseline_USD
is set during a TothBot process lifecycle. It is never
reset on reconnection. It resets only on full process
restart.

Start ping task and zombie detection on the private WS
connection.

------------------------------------------------------------
SS-START-006  STEP 6 — Reconcile Open Positions
------------------------------------------------------------

Process the snap_orders snapshot received from the
executions channel subscription in Step 5.

Restore the Position Mirror from the snapshot. For each
open position found, record: symbol, entry_fill_price
(from avg_price field), quantity, TP order_id, emergSL
order_id, cl_ord_id.

If any position has a TP order but no emergSL order, or
an emergSL order but no TP order: log INCOMPLETE_PROTECTION
at CRITICAL level and alert alert@tothbot.com. Do not
attempt to auto-repair the missing order. Position state
is ambiguous — operator confirmation is required before
any automated action.

Fallback: if the executions channel snapshot contains
a sequence gap (snap_orders data is incomplete): fall back
to REST GetOpenOrders for position reconciliation, and
REST GetAccountBalance for balance verification (AR-021).

------------------------------------------------------------
SS-START-007  STEP 7 — Restore Ticker Subscriptions
------------------------------------------------------------

For each pair with an open position in the Position Mirror:
  Subscribe ticker with event_trigger:bbo.
  This activates real-time MAE monitoring using bid price
  (the realizable exit price for long positions).

For all other monitored pairs:
  Subscribe ticker with event_trigger:trades.
  This provides volume data for Gate 2 and candle updates.

Ticker subscriptions are per-pair and managed individually.
The WS Manager tracks which event_trigger is active for
each pair.

------------------------------------------------------------
SS-START-008  STEP 8 — REST Warm-Up (GetOHLCData)
------------------------------------------------------------

For all WARM_UP pairs, execute REST GetOHLCData calls to
seed the indicator caches. Three calls are required per
pair, staggered 1.1 seconds apart within the same pair
to avoid rate limit violations. Calls for different pairs
may run concurrently.

  Call A — interval=5 (5-minute candles): seed ATR(14).
    Use response[:-1] only. The last entry (response[-1])
    is the current not-yet-committed candle. Including it
    corrupts ATR and all regime calculations (AR-017).

  Call B — interval=60 (1-hour candles): seed 1H EMA(20)
    and EMA(50) for Gate 4 HTF confirmation cache.
    Use response[:-1] only.

  Call C — interval=1440 (daily candles): compute ADX(14),
    EMA(20), EMA(50) for daily regime classification.
    Use response[:-1] only.

A pair transitions from WARM_UP to READY when all three
of the following are true:
  (a) ATR(14) seeded (from Call A, or after 14 candle
      close events if REST failed)
  (b) 1H EMA(20) and EMA(50) seeded (from Call B)
  (c) Daily regime classified (from Call C)

Pairs where REST warm-up fails remain in WARM_UP and seed
incrementally via WS candle close events. This is slower
but acceptable. TothBot does not wait for all pairs —
pipeline begins as soon as Step 9 is satisfied.

------------------------------------------------------------
SS-START-009  STEP 9 — First READY Pair: Enter OPERATIONAL
------------------------------------------------------------

TothBot V2 enters OPERATIONAL state when at least one
pair reaches READY state.

Send sd_notify READY=1 to systemd at this moment. systemd
transitions the service from activating to active.

WARM_UP pairs continue seeding in the background and
transition to READY organically. BLOCKED pairs are re-
evaluated on each instrument channel update event.

Pipeline evaluation runs only for READY pairs. WARM_UP
and BLOCKED pairs are excluded at the pipeline level
before Gate 1.

------------------------------------------------------------
SS-START-010  STEP 10 — Continuous Operation
------------------------------------------------------------

TothBot V2 operates continuously from OPERATIONAL state.

Daily at 00:00 UTC: the Regime Engine calls GetOHLCData
(interval=1440) for all monitored pairs to refresh the
daily regime classification. Calls are staggered to avoid
rate limit violations (AR-036).

The systemd watchdog task sends sd_notify WATCHDOG=1
every 30 seconds. systemd detects a frozen event loop
if no ping arrives within 120 seconds and restarts
TothBot automatically.

On restart: the full 10-step startup sequence executes
from Step 1.

============================================================
5. MID-SESSION RECONNECTION
============================================================

A WebSocket disconnection during live operation triggers
a reconnection sequence that is distinct from startup.
The reconnection sequence is specified in 0511001
WS_Manager_Specification.

The following invariants MUST be preserved across every
reconnection event. They are stated here as operational
requirements — they are never negotiable:

  INVARIANT 1: portfolio_baseline_USD is NEVER reset
    during reconnection. It was set once at startup
    (Step 5). It survives all reconnection events. It
    resets only on full TothBot process restart.

  INVARIANT 2: drawdown_pct is NEVER reset during
    reconnection. Drawdown is measured continuously from
    the startup baseline. Reconnection does not restart
    the drawdown clock.

  INVARIANT 3: system_state (NORMAL, SESSION_PAUSE,
    FULL_HALT) is PRESERVED across reconnection. A halt
    condition that existed before disconnect is still
    active after reconnect.

  INVARIANT 4: Any position that closed during the
    disconnection gap (TP filled or emergSL fired while
    offline) must be detected via Position Mirror vs
    snap_orders reconciliation. The CIATS Trade Outcome
    Bus must be fired for each gap-closed position before
    the pipeline resumes.

  INVARIANT 5: candle close events that fired during
    the reconnection gap are DISCARDED. The system does
    not attempt to replay missed pipeline evaluations.

============================================================
6. SHUTDOWN PROCEDURE
============================================================

------------------------------------------------------------
SS-STOP-001  NORMAL SHUTDOWN
------------------------------------------------------------

A normal shutdown is one where TothBot is stopped
deliberately by Bill with no positions open.

Preferred sequence:
  1. Wait for all open positions to close naturally
     (TP fills, signal-based exits, or MAE exits).
  2. Confirm Position Mirror is empty.
  3. Confirm no pending orders in Pending Order Registry.
  4. Stop TothBot process (systemctl stop tothbot).
  5. TothBot sends graceful WS close on both connections.
  6. systemd logs service stop.

------------------------------------------------------------
SS-STOP-002  SHUTDOWN WITH OPEN POSITIONS
------------------------------------------------------------

If TothBot must be stopped while positions are open
(maintenance, migration, emergency):

  MANDATORY BEFORE STOPPING:
  (a) Confirm that an active emergSL order exists on the
      Kraken matching engine for every open position.
      The emergSL provides crash protection during the
      offline period. TothBot must never be stopped
      with an unprotected open position.
  (b) Verify emergSL orders via REST GetOpenOrders or
      Kraken web interface before stopping the process.

  PROHIBITED:
  cancel_all_orders_after (DMS) must NEVER be called
  before shutdown. Calling DMS cancels the emergSL orders
  that protect open positions — this removes crash
  protection entirely. DMS is prohibited in all TothBot
  V2 code under any circumstance (AR-001, AR-055).

  AFTER STOPPING:
  Open positions remain protected by the resting emergSL
  orders on the Kraken matching engine. These orders
  remain active regardless of TothBot's process state.
  Bill monitors positions manually during any offline
  period.

------------------------------------------------------------
SS-STOP-003  CRASH OR WATCHDOG RESTART
------------------------------------------------------------

On crash or watchdog-triggered restart, systemd
automatically restarts TothBot after RestartSec=5 seconds.

The full 10-step startup sequence executes on restart.
Position Mirror is restored from snap_orders in Step 6.
Any positions that closed during the downtime gap are
detected and CIATS Trade Outcome Bus is fired for each.

During the restart window, all open positions remain
protected by their resting emergSL orders on the Kraken
matching engine. These orders fire automatically if the
stop-price is reached — no TothBot process required.

------------------------------------------------------------
SS-STOP-004  FULL HALT SHUTDOWN
------------------------------------------------------------

A Full Halt (drawdown >= 10% of portfolio_baseline_USD)
does not stop the TothBot process. It halts new entry
pipeline evaluations only. Existing positions continue
to be monitored and exited by the Exit Controller.

Bill decides whether to stop the TothBot process after
a Full Halt. If Bill stops TothBot after a Full Halt,
SS-STOP-002 procedure applies.

============================================================
7. HARD RULES
============================================================

  HR-SS-001  No pipeline evaluation may occur before at
             least one pair reaches READY state (Step 9).

  HR-SS-002  portfolio_baseline_USD is set ONCE at startup
             Step 5. It is never reset on reconnection.
             It resets only on full process restart.

  HR-SS-003  snap_orders: true is the PREFERRED reconciliation
             method at startup and reconnect. REST
             GetOpenOrders is the fallback only (AR-003).

  HR-SS-004  order_status: true is MANDATORY on every
             executions channel subscription. It must never
             be omitted (AR-026).

  HR-SS-005  response[-1] (the uncommitted current candle)
             must ALWAYS be excluded from all GetOHLCData
             calculations. Use response[:-1] only (AR-017).

  HR-SS-006  cancel_all_orders_after (DMS) is PROHIBITED
             in all TothBot V2 code under any circumstance.
             DMS cancels emergSL orders and destroys crash
             protection (AR-001, AR-055).

  HR-SS-007  TothBot must never be stopped with an open
             position that has no emergSL order active on
             the Kraken matching engine.

  HR-SS-008  Incomplete position protection (TP without
             emergSL or vice versa) must be logged CRITICAL
             and alerted. It must not be auto-repaired
             without operator confirmation.

  HR-SS-009  sd_notify READY=1 is sent only after Step 9
             (at least one READY pair). Not before.

  HR-SS-010  The 10-step startup sequence executes in full
             on every TothBot process start including
             crash restarts. No step is skippable.

============================================================
8. RELATED DOCUMENTS
============================================================

  0200000  Operations                    (parent Domain Doc)
  0210000  Operations_CIATS_Anchor       (parent anchor)
  0211002  Operations_Infrastructure_Specification dv1_0
  0511001  WS_Manager_Specification      dv1_0
  0511005  Position_Mirror_Specification dv1_0
  0500000  System_Architecture_Overview  dv1_18
             AR-003, AR-017, AR-021, AR-026, AR-049,
             AR-050, AR-055
  1011014  Startup_Sequence_Coding_Spec  dv1_2
  0411001  Kraken_WS_v2_Specification    dv1_11
  0311001  Namespace_Specification       (governs DCN)
  0311002  Document_Creation_Workflow    (governs sequence)

============================================================
9. REVISION HISTORY
============================================================

  dv1_0  04-05-2026  Initial document. Complete startup and
                     shutdown procedure. Three pair states
                     defined (WARM_UP, READY, BLOCKED).
                     10-step startup sequence (SS-START-001
                     through SS-START-010) per AR-049.
                     Step 1: Status API check. Step 2: WS
                     token. Step 3: Public WS + subscribe.
                     Step 4: Instrument snapshot + pair
                     states. Step 5: Private WS + executions
                     + balances + portfolio_baseline_USD.
                     Step 6: snap_orders reconciliation,
                     INCOMPLETE_PROTECTION rule, REST
                     fallback. Step 7: Ticker restore per
                     position state. Step 8: REST warm-up
                     GetOHLCData (3 calls per pair). Step
                     9: First READY pair + READY=1. Step 10:
                     Continuous operation + daily regime
                     refresh. Five reconnection invariants
                     (portfolio_baseline_USD, drawdown_pct,
                     system_state, gap-closed positions,
                     discarded candles). Shutdown procedure:
                     normal (SS-STOP-001), with open
                     positions (SS-STOP-002), crash/watchdog
                     (SS-STOP-003), Full Halt (SS-STOP-004).
                     DMS prohibition stated. Hard rules
                     HR-SS-001 through HR-SS-010 defined.

============================================================
END OF DOCUMENT
============================================================
