DocDCN:     0211002
DocTitle:   Operations_Infrastructure_Specification
DocVersion: dv1_2
DocOwner:   Bill
DocPath:    GitHub.com/TothBot/TothBot_V2-Docs/02XXXXX_Operations/0211002_Operations_Infrastructure_Specification_dv1_2.txt
DocDate:    05-14-2026
DocTime:    23:59:59 UTC

============================================================
OPERATIONS INFRASTRUCTURE SPECIFICATION
============================================================

============================================================
1. PURPOSE
============================================================

This document defines the complete operational infrastructure
requirements for TothBot V2. It specifies the VPS
environment, process management, network requirements,
API key security, alerting, log management, and all
standing operational constraints.

Infrastructure reliability is not optional. A crashed
and unmonitored TothBot V2 with open positions is a
direct loss event. Every infrastructure requirement in
this document exists to prevent that outcome.

This document is the parent specification for AA=10
coding specification 1011013 VPS_Deployment_Coding_Spec.
(Note: 1011013 dv1_6 was RETIRED in TB00228 RANK 7
retirement cascade. Per DD-001/DD-002 diagrams-as-
canonical, 0211002 plus 0500000 diagrams D7 + D8 + root
<desc> governs: list contract identifiers — including
contract:VD-SYS-008 systemd StartLimit defense — are the
sole canonical authority for VPS deployment content
post-retirement.)

============================================================
2. PARENT CHAIN
============================================================

  0000000  Ethics_and_Safety              (Domain)
  0100000  Management                     (Domain)
  0200000  Operations                     (Domain Document)
  0210000  Operations_CIATS_Anchor        (Sub-Domain Anchor)
  0211002  This document                  (Instance)

============================================================
3. VPS ENVIRONMENT
============================================================

------------------------------------------------------------
IN-VPS-001  CURRENT VPS
------------------------------------------------------------

  Provider:   Hetzner Cloud
  Type:       CPX22 (2 vCPU, 4GB RAM, 40GB disk)
  Location:   Ashburn, VA
  IP:         87.99.141.44
  OS:         Ubuntu 24.04.4 LTS
  Python:     3.12.3
  SSH access: ssh root@87.99.141.44
  SSH key:    C:\Users\wstot\.ssh\id_ed25519

------------------------------------------------------------
IN-VPS-002  RECOMMENDED MIGRATION — HILLSBORO, OR
------------------------------------------------------------

Kraken Spot matching engine runs on AWS us-west-2
(Oregon). The current Ashburn, VA location adds
unnecessary round-trip latency to every order submission
and execution confirmation.

Recommended migration: Hetzner Cloud CPX22, Hillsboro, OR.
Rationale: ~50-70ms round-trip improvement vs Ashburn.
At 5-minute candle frequency this has no strategy impact
but reduces execution risk in fast market conditions by
delivering order confirmations faster.

Migration timing: evaluate at paper trading start.
IP whitelist on both Kraken API keys MUST be updated to
the new IP before any VPS migration is executed.
Migration without IP whitelist update will lock TothBot
out of all private API calls.

Note: Hetzner US locations support CPX/CCX series only.
US pricing is higher than EU. Confirm CPX22 pricing
in Hillsboro before committing to migration.

------------------------------------------------------------
IN-VPS-003  MINIMUM RESOURCE REQUIREMENTS
------------------------------------------------------------

TothBot V2 minimum requirements on CPX22:
  RAM:   4GB (adequate for asyncio event loop, 20 positions,
              Logger queue 10,000 entries, CIATS corpus)
  CPU:   2 vCPU (adequate for asyncio single-threaded
              hot path + ThreadPoolExecutor for CPU-bound
              statistical computations)
  Disk:  40GB (adequate for log retention per IN-LOG-001)
  Network: Hetzner standard — adequate for WS + REST

CPX22 is validated as sufficient for TothBot V2.
No upgrade is required unless CIATS data volumes grow
significantly beyond initial projections.

============================================================
4. PROCESS MANAGEMENT
============================================================

------------------------------------------------------------
IN-PM-001  TOTHBOT MUST RUN AS A SYSTEMD SERVICE
------------------------------------------------------------

TothBot V2 must be managed by systemd. Manual process
management is prohibited in production. systemd provides:
  - Automatic restart on crash (Restart=on-failure)
  - Automatic start on VPS reboot (WantedBy=multi-user.target)
  - Watchdog monitoring of event loop health
  - Structured startup/shutdown lifecycle
  - Integration with system journal logging

------------------------------------------------------------
IN-PM-002  SYSTEMD SERVICE REQUIREMENTS
------------------------------------------------------------

Required systemd unit configuration:

  [Unit]
  Description=TothBot V2 Trading System
  After=network-online.target
  Wants=network-online.target

  [Service]
  Type=notify
  User=root
  WorkingDirectory=/root/TothBot_V2-Code
  EnvironmentFile=/root/.tothbot.env
  ExecStart=/usr/bin/python3 -m tothbot
  Restart=on-failure
  RestartSec=5
  WatchdogSec=120
  LimitNOFILE=65535

  [Install]
  WantedBy=multi-user.target

Requirements explained:
  After=network-online.target: TothBot does not start
    until the network is confirmed available. Prevents
    startup failures caused by boot-time race conditions.
  Type=notify: TothBot signals systemd when it is
    actually ready (READY=1 via sd_notify). systemd
    does not mark the service as started until TothBot
    confirms readiness after full startup validation.
  EnvironmentFile: API keys and secrets are loaded from
    /root/.tothbot.env — never hardcoded in source code.
  Restart=on-failure with RestartSec=5: automatic restart
    on crash after 5-second delay. Prevents restart storm.
  WatchdogSec=120: systemd restarts TothBot if no watchdog
    ping is received within 120 seconds. Detects frozen
    event loops that do not crash outright.
  LimitNOFILE=65535: prevents EMFILE errors under high
    WebSocket subscription load across many monitored pairs.

------------------------------------------------------------
IN-PM-003  SYSTEMD WATCHDOG REQUIREMENT
------------------------------------------------------------

TothBot V2 must implement an internal watchdog loop that
sends a systemd watchdog ping every 30 seconds. This is
separate from and in addition to the systemd WatchdogSec
timer.

The watchdog loop runs as a persistent asyncio Task.
If the event loop hangs, the watchdog Task stops sending
pings. systemd detects the absence of pings after 120
seconds and restarts TothBot.

The 30-second ping interval with a 120-second watchdog
timeout provides 4 missed pings before restart — enough
tolerance for brief event loop delays without triggering
spurious restarts.

------------------------------------------------------------
IN-PM-004  STARTUP NOTIFICATION REQUIREMENT
------------------------------------------------------------

TothBot V2 must send sd_notify READY=1 after all startup
validation steps complete successfully. Specifically,
READY=1 is sent only after:
  - Both WebSocket connections established
  - Position Mirror reconciled (snap_orders or REST)
  - Parameter Store loaded
  - CIATS initialized
  - All startup gates passed

systemd holds the service in activating state until
READY=1 is received. This prevents dependent services
from starting before TothBot is actually ready.

============================================================
5. NETWORK REQUIREMENTS
============================================================

------------------------------------------------------------
IN-NET-001  OUTBOUND CONNECTIVITY REQUIREMENTS
------------------------------------------------------------

TothBot V2 requires persistent outbound connectivity to:

  Kraken Public WS v2:
    wss://ws.kraken.com/v2
    Port 443 (WSS). Persistent connection. 24/7.

  Kraken Private WS v2:
    wss://ws-auth.kraken.com/v2
    Port 443 (WSS). Persistent connection. 24/7.

  Kraken REST API:
    https://api.kraken.com
    Port 443 (HTTPS). Periodic calls. Not persistent.

  Kraken Status API:
    https://status.kraken.com/api/v2/scheduled-maintenances.json
    Port 443 (HTTPS). Called at startup only.

  Email alerts (SMTP):
    alerts@tothbot.com outbound. SMTP port as configured.
    Plural address — real Proton Mail mailbox per OI-019.

------------------------------------------------------------
IN-NET-002  CONNECTION FAILURE BEHAVIOR
------------------------------------------------------------

WebSocket disconnection is a normal operational event
that must be handled gracefully per the reconnection
specification in 0511001 WS_Manager_Specification.

TothBot V2 must never halt on a transient WS disconnection.
It must reconnect automatically per the reconnection
procedure. Emergency SL orders on the Kraken matching
engine provide crash protection during any disconnection
period.

------------------------------------------------------------
IN-NET-003  CLOUDFLARE RECONNECT CONSTRAINT
------------------------------------------------------------

Kraken WS endpoints are served through Cloudflare.
Cloudflare enforces connection limits per IP. Rapid
reconnection cycling can trigger rate limiting.
Reconnection backoff must be implemented per 0511001.
Minimum reconnect delay: 5 seconds. Maximum: per
backoff schedule in 0511001.

============================================================
6. API KEY SECURITY
============================================================

------------------------------------------------------------
IN-KEY-001  TWO API KEY PAIRS REQUIRED
------------------------------------------------------------

TothBot V2 requires exactly two Kraken API key pairs:
  DATA key:  market data access only. No order permissions.
  TRADE key: order submission and account access.

The two-key architecture limits blast radius: a compromise
of the DATA key cannot submit orders.

------------------------------------------------------------
IN-KEY-002  IP WHITELISTING — MANDATORY
------------------------------------------------------------

Both the DATA key and the TRADE key MUST be IP-whitelisted
to the VPS IP address in the Kraken account settings.
This is not optional. A non-whitelisted TRADE key is a
security exposure — any party with the key string can
submit orders.

IP whitelist must be updated BEFORE any VPS migration.
Executing a VPS migration without updating the whitelist
will immediately lock TothBot out of all private API calls
with open positions potentially unmonitored.

Sequence for VPS migration:
  1. Provision new VPS, obtain new IP.
  2. Update Kraken API key whitelist to new IP.
  3. Verify connectivity from new VPS.
  4. Migrate TothBot to new VPS.
  5. Decommission old VPS.

------------------------------------------------------------
IN-KEY-003  ENVIRONMENT FILE STORAGE
------------------------------------------------------------

API keys, secrets, and all credentials are stored in
/root/.tothbot.env on the VPS. This file is:
  - Loaded by systemd via EnvironmentFile directive.
  - Never committed to any Git repository.
  - Never logged in any log file.
  - Never transmitted in any alert message.
  - Readable only by root (chmod 600).

API keys are never hardcoded in source code. Any source
file containing an API key literal is a critical defect.

------------------------------------------------------------
IN-KEY-004  NONCE WINDOW
------------------------------------------------------------

Kraken API nonce window: 10,000ms (10 seconds).
Both DATA and TRADE key nonce windows must be set to
10,000ms in Kraken account settings. This accommodates
clock drift and retry delays without nonce collisions.

============================================================
7. ALERTING
============================================================

------------------------------------------------------------
IN-ALT-001  ALERT DESTINATION
------------------------------------------------------------

All TothBot V2 operational alerts are sent to:
  alerts@tothbot.com

Plural address. This is the real Proton Mail mailbox per
OI-019. The singular `alert@tothbot.com` cannot be created
in Proton Mail (catch-all reservation) — every alert
addressed to the singular form bounces. Do not use
singular form anywhere in code, config, documents, or
alert payloads.

No other alert destination is used. CIATS does not send
alerts directly — all alerts flow through the Logger
per 0511006 Section 7.

------------------------------------------------------------
IN-ALT-002  ALERT LEVELS
------------------------------------------------------------

Alert severity levels (per 0511006):
  INFO:     Informational. No action required.
            Example: startup complete, daily regime update.
  WARNING:  Degraded condition. Monitor.
            Example: WS reconnection, CUSUM signal.
  CRITICAL: Immediate operator attention required.
            Example: negative Kelly, full halt drawdown,
            Logger queue full, VPS resource exhaustion.

CRITICAL alerts require operator acknowledgment.
TothBot V2 does not auto-resolve CRITICAL alerts.

------------------------------------------------------------
IN-ALT-003  KRAKEN STATUS API CHECK AT STARTUP
------------------------------------------------------------

TothBot V2 must query the Kraken Status API at every
startup to check for scheduled maintenance windows:
  https://status.kraken.com/api/v2/scheduled-maintenances.json

If a scheduled maintenance is within 2 hours of startup:
  Log at CRITICAL level. Send alert to alerts@tothbot.com.
  Continue startup — this check is informational only.
  It does not block startup.

Rationale: Forewarned maintenance allows Bill to decide
whether to proceed with live trading or stand down until
the maintenance window passes.

============================================================
8. LOG MANAGEMENT
============================================================

------------------------------------------------------------
IN-LOG-001  LOG RETENTION REQUIREMENTS
------------------------------------------------------------

TothBot V2 generates two log streams:
  System journal: managed by systemd/journald.
  TothBot log files: managed by the Logger (0511006).

TothBot log file retention:
  Minimum retention: 90 days of trading logs.
  Rotation: daily. Compressed after rotation.
  Location: /root/TothBot_V2-Code/logs/ (or as configured)

CIATS data retention:
  Trade Outcome Bus records: retained indefinitely.
  These are the zero-cost historical library. They must
  never be deleted — they are the foundation of all
  regime-segmented optimization after 600 trades.
  Stored in: /root/TothBot_V2-Code/data/ (or as configured)

------------------------------------------------------------
IN-LOG-002  LOGROTATE REQUIREMENT
------------------------------------------------------------

logrotate must be configured and active before the first
live trading run. Required configuration:

  /root/TothBot_V2-Code/logs/*.log {
      daily
      rotate 90
      compress
      delaycompress
      missingok
      notifempty
      copytruncate
  }

copytruncate: allows log rotation without restarting
TothBot — rotation is performed on the active file
without interrupting the Logger's file handle.

------------------------------------------------------------
IN-LOG-003  LOGGER QUEUE ALERT REQUIREMENT
------------------------------------------------------------

The Logger maintains an async bounded queue (maxsize=10,000
per 0511006). If the queue fills, an immediate CRITICAL
alert must be sent to alerts@tothbot.com. The Logger must
never silently drop events. A full queue indicates either
a disk I/O problem or an unexpectedly high event rate —
both require immediate operator attention.

============================================================
9. STANDING OPERATIONAL CONSTRAINTS
============================================================

------------------------------------------------------------
IN-OC-001  24/7/365 OPERATION
------------------------------------------------------------

TothBot V2 operates 24/7/365. Kraken Spot trades
continuously. There are no scheduled maintenance windows,
no planned downtime windows, and no trading hours. Any
planned maintenance or upgrade requires:
  (a) Waiting for all positions to close, OR
  (b) Manual position management by Bill before stopping
      TothBot.
  (c) Confirming emergency SL orders are active on the
      Kraken matching engine before stopping TothBot.

------------------------------------------------------------
IN-OC-002  TOTHBOT RESTART IS AN OPERATIONAL EVENT
------------------------------------------------------------

Every TothBot restart — whether from crash, manual stop,
or watchdog timeout — must be logged as an operational
event with timestamp and cause. Restarts are not routine
maintenance — they are incidents requiring review.

Frequent restarts (more than 3 per 24-hour period) are
a CRITICAL operational alert condition.

------------------------------------------------------------
IN-OC-003  VPS RESOURCE MONITORING
------------------------------------------------------------

The following VPS resource thresholds are CRITICAL alert
conditions:
  RAM utilization:  > 80% of 4GB = > 3.2GB
  CPU utilization:  > 80% sustained over 5 minutes
  Disk utilization: > 80% of 40GB = > 32GB
  Network:          any sustained packet loss > 1%

These thresholds are CIATS-owned starting values. CIATS
monitors ws_roundtrip_latency_ms (Stream 1 Signal 3) as
a proxy for network health. Direct VPS resource monitoring
is a future infrastructure enhancement (not in V1 scope).

------------------------------------------------------------
IN-OC-004  NO LIVE TRADING WITHOUT EMERGENCY SL ACTIVE
------------------------------------------------------------

TothBot V2 must never hold an open position without an
active emergency SL order on the Kraken matching engine.
The emergency SL is crash protection — it fires only if
TothBot fails to manage the exit. It must be present at
all times for all open positions.

Verification: confirmed at startup via snap_orders
reconciliation (AR-003). Any position without a
corresponding emergency SL order is a CRITICAL defect
requiring immediate remediation.

============================================================
10. HARD RULES
============================================================

  HR-IN-001  TothBot V2 runs as a systemd service.
             Manual process management is prohibited
             in production.

  HR-IN-002  Both API keys MUST be IP-whitelisted to the
             VPS IP. Non-whitelisted TRADE key is a
             critical security exposure.

  HR-IN-003  API keys are stored in EnvironmentFile only.
             Never in source code. Never in logs.

  HR-IN-004  IP whitelist MUST be updated BEFORE any
             VPS migration. Sequence: update whitelist,
             verify, migrate.

  HR-IN-005  logrotate MUST be configured and active
             before the first live trading run.

  HR-IN-006  CIATS Trade Outcome Bus records are retained
             indefinitely. They must never be deleted.

  HR-IN-007  TothBot must never hold an open position
             without an active emergency SL order on
             the Kraken matching engine.

  HR-IN-008  A full Logger queue is a CRITICAL alert.
             Events must never be silently dropped.

  HR-IN-009  Frequent restarts (>3 per 24h) are a
             CRITICAL alert condition requiring
             immediate investigation.

============================================================
11. RELATED DOCUMENTS
============================================================

  0200000  Operations                    (parent Domain Doc)
  0210000  Operations_CIATS_Anchor       (parent anchor)
  0211001  CIATS_Specification           dv1_0
  0511001  WS_Manager_Specification      dv1_6
  0511006  Logger_Specification          dv1_2
  0500000  System_Architecture_Overview  dv1_72
  0311001  Namespace_Specification       (governs DCN)
  0311002  Document_Creation_Workflow    (governs sequence)

(Note: 1011013 VPS_Deployment_Coding_Spec dv1_6 was
RETIRED in TB00228 RANK 7 retirement cascade and removed
from this Related Documents list. Per DD-001/DD-002,
0500000 D7 + D8 SVG diagrams + contract:VD-SYS-008 in
root <desc> governs: list are the canonical authority
for VPS deployment content downstream of this parent
specification.)

============================================================
12. REVISION HISTORY
============================================================

  dv1_2  05-14-2026  TB00255 AMENDMENT-17 §3.2 — F-18
                     RESIDUAL MULTI-CITATION VERSION-LAG
                     CLOSURE (PROSE-UPDATE-BR(d)).

                     Three §11 RELATED DOCUMENTS version
                     citations refreshed to current
                     authoritative versions per TB00255
                     NSI §3.2 + TB00254 SL §6 F-18
                     PARTIALLY-CLOSED carry-forward + TB00209
                     Gate 4 PROSE-UPDATE-BR(d) recommendation:

                       0500000 dv1_56 -> dv1_72 (16 lag).
                       0511001 dv1_0  -> dv1_6  (6 lag).
                       0511006 dv1_0  -> dv1_2  (2 lag).

                     0211001 dv1_0 line preserved unchanged
                     (current authoritative version). 1011013
                     RETIRED note preserved.

                     REASON FOR CHANGE.
                     Stale-citation refresh per RR-SCOPE-005
                     mechanical derivable execution (TB00255
                     chat-turn 1 directive bundled with
                     §3.1 universal parent-chain-completeness
                     rule ratification under Bill standing
                     "do not pause" directive). Loss-prevention
                     rationale: stale citations cause future
                     cold-load sessions and downstream readers
                     to follow links to outdated content;
                     refresh restores citation freshness.

                     FP/DP ANALYSIS.
                     Option A (PROSE-UPDATE-BR(d) mechanical
                       refresh) ACCEPTED. TB00209 Gate 4
                       analysis: stale citations don't directly
                       cause loss but cost cold-load accuracy;
                       fix is preventive; mechanical with no
                       semantic decision required.
                     Option B (defer) REJECTED. Each session
                       under the gap recurrence-tests the
                       carry-forward; mechanical fix is low
                       cost.

                     Decision: Option A.

                     PARENT CHAIN COMPLIANCE.
                     0211002 chain: 0211002 (Tier 3 instance)
                     -> 0210000 (Tier 2 anchor) -> 0200000
                     (Tier 1 domain). All parents present.
                     Per DD-012 0500000-canonical-all-docs,
                     0500000 dv1_72 is the canonical reference
                     for content downstream of this parent
                     specification.

                     CLOSES.
                     F-18 RESIDUAL MULTI-CITATION VERSION-LAG
                     (TB00254 SL §6 F-18 PARTIALLY-CLOSED
                     carry-forward; TB00255 NSI §3.2;
                     MDR v1_14 DR-044 §7(a) carry-forward).
                     Cumulative F-18 cite count = 0 at this
                     amendment close.

                     COMPANION DELIVERABLES THIS SESSION
                     (TB00255 SESSION-CLOSE):
                       0311001 v1_16 -> v1_17.txt (§3.1
                         universal parent-chain-completeness
                         rule codification — V-039 NEW).
                       0211002 dv1_1 -> dv1_2.txt (this
                         document — §3.2 F-18 closure).
                       TB00153 MDR v1_14 -> v1_15.txt
                         (DR-045 NEW rule codification entry +
                         DR-046 NEW F-18 closure entry).
                       TB00255 Session_Log v1_0 (session-close
                         trio member 1).
                       TB00256 Next_Session_Instructions v1_0
                         (session-close trio member 2).
                       OCS chat-response copy box (session-
                         close trio member 3).

                     Document Status: this document is the
                     sole authoritative version of 0211002
                     post-supersession; dv1_1 is superseded.

  dv1_1  05-12-2026  OI-019 PLURAL ADDRESS PROPAGATION +
                     1011013 RETIREMENT-CASCADE COMPANION
                     AMENDMENT (TB00228 RANK 7 PHASE 2).

                     Two amendment classes applied to dv1_0
                     baseline:

                     CLASS A — OI-019 plural address fix.
                     All five sites referencing the singular
                     non-existent `alert@tothbot.com`
                     mailbox updated to the plural real
                     `alerts@tothbot.com` mailbox per OI-019
                     (the defect first identified TB00115
                     2026-04-20 and fixed in 1011013 dv1_5
                     + logger.py dv1_3 + startup_sequence
                     dv1_8 + vps_deployment dv1_5; this
                     amendment propagates the same fix to
                     the parent specification 0211002 which
                     was not amended in TB00115). Sites
                     amended:
                       §5 IN-NET-001 SMTP outbound address.
                       §7 IN-ALT-001 alert destination.
                       §7 IN-ALT-003 Kraken Status API
                         CRITICAL alert routing.
                       §8 IN-LOG-003 Logger queue-full
                         CRITICAL alert routing.

                     CLASS B — 1011013 retirement-cascade
                     companion notes.
                       §1 Purpose paragraph: clarifying
                         note that 1011013 dv1_6 was
                         retired in TB00228 RANK 7 cascade
                         and that 0211002 + 0500000
                         diagrams are sole canonical
                         authority for VPS deployment
                         content post-retirement.
                       §11 Related Documents: 1011013
                         entry removed (file no longer
                         exists at GitHub HEAD after Phase
                         3 git rm); 0500000 reference
                         updated from "dv1_18" stale
                         pointer to current "dv1_56";
                         retirement note added.

                     HEADER REFRESH.
                     DocVersion: dv1_0 -> dv1_1.
                     DocPath: ...dv1_0.txt -> ...dv1_1.txt.
                     DocDate: 04-05-2026 -> 05-12-2026.
                     DocTime: refreshed.

                     SOURCE.
                     OI-019 defect record: TB00115 SL
                     2026-04-20 forensic + TB00000 v2_10
                     §12 revision history entry. RR-SCOPE-
                     009 four-question Chesterton's-fence
                     audit performed at TB00228 Phase 0
                     surfaced the gap: 1011013 dv1_6 used
                     plural alerts@tothbot.com but parent
                     0211002 dv1_0 still used singular
                     alert@tothbot.com. Retirement of
                     1011013 without propagating the OI-019
                     fix to 0211002 would leave a non-
                     existent mailbox as the canonical
                     alert destination — direct violation
                     of RR-SCOPE-009 Q4 (loss-prevention
                     property preserved) and direct
                     reintroduction of the original OI-019
                     defect.

                     REASON FOR CHANGE.
                     Phase 2 of TB00228 RANK 7 retirement
                     cascade for 1011013 dv1_6. Option C
                     selected per FP/DP analysis at TB00228
                     chat-turn cold-load (RR-SCOPE-009
                     compliance mandates plural address
                     propagation to parent before child
                     retirement; only one compliant option
                     remained after RR-SCOPE-009 audit;
                     executed per DD-011 derivable-decision
                     authority).

                     FP/DP ANALYSIS.
                     Option A (retire 1011013 without
                       amending 0211002) — REJECTED.
                       RR-SCOPE-009 Q4 violation; would
                       leave non-existent mailbox as
                       canonical alert destination.
                     Option B (defer 0211002 amendment to
                       separate session, retire 1011013
                       now) — REJECTED. Same RR-SCOPE-009
                       violation; window of brokenness
                       between sessions unacceptable.
                     Option C (bundle 0211002 dv1_0 ->
                       dv1_1 with TB00228 RANK 7 cascade)
                       — ACCEPTED. RR-SCOPE-009 compliant.
                       Maintains zero-broken-alert
                       invariant.

                     Decision: Option C.

                     PARENT CHAIN COMPLIANCE.
                     0211002 chain: 0211002 (Tier 3
                     instance) -> 0210000 (Tier 2 anchor)
                     -> 0200000 (Tier 1 domain). All
                     parents present in project folder.
                     Per DD-001/DD-002 diagrams-as-
                     canonical, 0500000 D7 + D8 + root
                     <desc> governs: list (contract:
                     VD-SYS-008 + contract:WM-PACE-002 +
                     contract:WM-SHARD-001 + etc.) are
                     the diagrammatic canonical authority
                     for content downstream of 0211002.

                     COMPANION DELIVERABLES THIS SESSION
                     (TB00228 RANK 7 RETIREMENT CASCADE):
                       TB00000 v2_35 -> v2_36.txt (Phase 1
                         StartLimit parameter absorption
                         to §9.17).
                       0211002 dv1_0 -> dv1_1.txt (this
                         document — Phase 2 OI-019 plural
                         address propagation + retirement
                         companion notes).
                       0500000 dv1_55 -> dv1_56.html
                         (Phase 2 prose Assign-line
                         redirects).
                       TB00153 MDR v1_11 -> v1_12.txt
                         (Phase 2 Amendment 2: 1011013
                         retirement + OI-019 propagation
                         entry).
                       Phase 3 — 1011013 dv1_6 git rm +
                         commit + push copy box for Bill
                         execution.
                       TB00228 Session_Log v1_0 + TB00229
                         Next_Session_Instructions v1_0
                         (session close trio).

                     Document Status: this document is
                     the sole authoritative version of
                     0211002 post-supersession; dv1_0 is
                     superseded.

  dv1_0  04-05-2026  Initial document. Complete infrastructure
                     specification. VPS environment (IN-VPS-001
                     through IN-VPS-003): Hetzner CPX22,
                     Ashburn VA, migration recommendation to
                     Hillsboro OR. Process management (IN-PM-001
                     through IN-PM-004): systemd mandatory,
                     full unit file spec, watchdog loop
                     requirement, READY=1 notification.
                     Network (IN-NET-001 through IN-NET-003):
                     all endpoint requirements, connection
                     failure behavior, Cloudflare constraint.
                     API key security (IN-KEY-001 through
                     IN-KEY-004): two-key architecture,
                     IP whitelisting mandatory, environment
                     file storage, nonce window. Alerting
                     (IN-ALT-001 through IN-ALT-003): all
                     alerts to alert@tothbot.com, severity
                     levels, Kraken Status API startup check.
                     Log management (IN-LOG-001 through
                     IN-LOG-003): retention 90 days minimum,
                     logrotate spec, Logger queue alert.
                     Standing constraints (IN-OC-001 through
                     IN-OC-004): 24/7/365, restart as incident,
                     resource thresholds, emergency SL always
                     active. Hard rules HR-IN-001 through
                     HR-IN-009 defined.

============================================================
END OF DOCUMENT
============================================================
