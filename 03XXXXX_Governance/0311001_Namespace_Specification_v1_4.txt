DocDCN:     0311001
DocTitle:   Namespace_Specification
DocVersion: v1_4
DocOwner:   Bill
DocPath:    GitHub.com/TothBot/TothBot_V2-Docs/03XXXXX_Governance/0311001_Namespace_Specification_v1_4.txt
DocDate:    05-04-2026
DocTime:    18:30:00 UTC

============================================================
NAMESPACE SPECIFICATION
============================================================

============================================================
1. PURPOSE
============================================================

This document defines the complete namespace model for
TothBot V2. It governs how Document Control Numbers (DCNs)
are assigned, interpreted, and validated across all
controlled documents in the TothBot V2 document control
system.

This is a clean-slate specification. It shares no legacy
constraints with any prior TothBot namespace. All decisions
in this document are derived from first principles using
FP/DP analysis. Where this document conflicts with any
prior TothBot namespace document, this document governs.

============================================================
2. SCOPE
============================================================

This specification applies to ALL controlled documents and
code files in the TothBot V2 system, across BOTH repositories:

  TothBot_V2-Docs (github.com/TothBot/TothBot_V2-Docs)
    Scope: All specification and governance documents.
    File format: .txt (Claude project compatible).
    DC header format: Standalone 7-field header block.

  TothBot_V2-Code (github.com/TothBot/TothBot_V2-Code)
    Scope: All trading system code, operational support
    scripts, configuration files, and deployment files in
    the trading system repository.
    File format: .py, .sh, .txt, .ini, .service, and others.
    DC header format: 7-field header embedded as a comment
    block at the top of each file, using the file's native
    comment syntax (# for Python and bash, etc.).

HR-DC-001  ALL files in TothBot_V2-Code are controlled
           documents. There are no uncontrolled files
           in TothBot_V2-Code.

HR-DC-002  Every file in TothBot_V2-Code MUST have a
           7-field DC header. This is not optional.
           Missing or malformed headers are document
           control violations.

HR-DC-003  Every file in TothBot_V2-Code MUST be
           registered in 1010000 Section 5 Code File
           Registry. Unregistered files are violations.

HR-DC-004  Every file in TothBot_V2-Code MUST have a
           Revision History section in comment format.

Corrective action: This scope expansion corrects the
governance gap documented in TB00079 where the scope
limitation to TothBot_V2-Docs only was found to be
incorrect and was causing document control violations.

Any document outside both repositories is an uncontrolled
working copy and has no governing authority.

This specification does not apply to TothBot-Docs or
TothBot-Code (the original TothBot system). Those
repositories are permanent read-only archives governed
by their own namespace.

============================================================
3. GOVERNING REPOSITORIES
============================================================

TothBot V2 has exactly two repositories:

  TothBot_V2-Docs
    URL:    github.com/TothBot/TothBot_V2-Docs
    Local:  C:\Users\wstot\GitHub\TothBot\TothBot_V2-Docs\
    VPS:    ~/TothBot_V2-Docs
    Purpose: All controlled specification and governance
             documents. Pathway 1 (GitHub document control
             automation) runs here.
    Header: Standalone 7-field header block.

  TothBot_V2-Code
    URL:    github.com/TothBot/TothBot_V2-Code
    Local:  C:\Users\wstot\GitHub\TothBot\TothBot_V2-Code\
    VPS:    ~/TothBot_V2-Code
    Purpose: All trading system code, operational support
             scripts, and configuration files. Pathway 2
             (TothBot trading system) runs here. All files
             are controlled documents.
    Header: 7-field header in comment block. See Section 5.

    Subfolder layout:
      tothbot/      Python package (trading system modules).
      operations/   Operational support scripts (VPS
                    monitoring, deployment helpers,
                    maintenance utilities). Non-Python.
                    Parallel to tothbot/. Example:
                    operations/tothbot_sweep.sh.
      tests/        Test suites (pytest).

HR-001  The two repositories are completely independent.
        No code, configuration, or dependency is shared.

HR-002  TothBot_V2-Code contains only trading system
        code, operational support scripts, and their
        configuration. No document control logic permitted.

HR-003  TothBot_V2-Docs contains only controlled
        specification documents and document control
        automation code. No trading system logic
        permitted.

------------------------------------------------------------
3.1 PATH GOVERNANCE FOR CONTROLLED DOCUMENTS (HR-PATH)
------------------------------------------------------------

HR-PATH-001  The ONLY authoritative local filesystem
             paths for controlled documents are the two
             governed local repositories listed in
             Section 3:

               C:\Users\wstot\GitHub\TothBot\TothBot_V2-Docs\
               C:\Users\wstot\GitHub\TothBot\TothBot_V2-Code\

             No other local path may be used to stage,
             edit, or transfer any controlled document.

HR-PATH-002  "Controlled document" means any file
             governed under this Namespace Specification.
             This includes but is not limited to:
               - All files in TothBot_V2-Docs (.txt)
               - All files in TothBot_V2-Code (.py,
                 .sh, .gitignore, .ini, .service,
                 README.md, and all others per HR-DC-001)
               - All diagram assets (.svg) per Section 15
               - All controlled-document source files
                 (e.g., 0500000 Claude_Source.txt, .docx,
                 .pdf)

HR-PATH-003  Controlled code files and operational
             scripts (any file in TothBot_V2-Code) MUST
             NEVER be staged, edited, or transferred via
             any local path outside the governed local
             repo at:

               C:\Users\wstot\GitHub\TothBot\TothBot_V2-Code\

             In particular, scp from the VPS to any
             local path outside the governed repo is
             PROHIBITED for controlled code files and
             operational scripts. The authoritative
             source for all code and operational scripts
             is GitHub, mirrored into the governed local
             repo via git pull.

HR-PATH-004  If Claude is uncertain whether a given file
             is a controlled document under HR-PATH-002,
             Claude MUST treat the file as controlled.
             Only Bill may declare a file non-controlled.
             Given that HR-DC-001 establishes that ALL
             files in TothBot_V2-Code are controlled, no
             operational exception exists: any file
             placed in the repository is a controlled
             document requiring full DC compliance
             (7-field header, Revision History, registry
             entry in 1010000 Section 5).

HR-PATH-005  Per TB00000 v2_12 §4.2 ABSOLUTE STANDING
             RULE, the retired cloud-sync local path is
             PROHIBITED from appearing in any path,
             command, instruction, response, or artifact
             Claude produces. If a task requires a local
             path that is not in the authoritative path
             tree in TB00000 §11, Claude STOPS and asks
             Bill for the correct path before proceeding.
             Claude does not invent replacement paths.
             Claude does not carry forward paths from
             prior session logs.

Corrective action: Rules HR-PATH-001 through HR-PATH-005
close the path-governance gap identified as OI-033
(DEFECT-CLAUDE-DESKTOP-STAGING-PATH-001, originating
TB00112) and OI-039 (governance residual contamination,
originating TB00115). The prior HR-PATH-004 OPERATIONAL-
SCRIPT EXCEPTION from v1_2 has been eliminated: operational
scripts (such as /root/tothbot_sweep.sh on the VPS) are
now first-class controlled documents living in the
TothBot_V2-Code operations/ subfolder, deployed via
git pull, and governed by the same rules as all other
TothBot_V2-Code files. This eliminates the exception
vector that was the source of recurring prohibited-string
references across governance, specs, and session logs.

============================================================
4. DCN STRUCTURE
============================================================

All DCNs follow the format: AABCDDD

  AA  = Domain (2 digits, 00–11)
  B   = Sub-Domain level (1 digit, 0–9)
  C   = Instance level (1 digit, 0–1)
  DDD = Sequential instance number within sub-domain

------------------------------------------------------------
4.1 DOCUMENT TIER DEFINITIONS
------------------------------------------------------------

  Tier 1 — Domain Document (B=0, C=0, DDD=000)
    IS the domain. One per domain. Anchors all sub-domains.
    Example: 0300000 Governance

  Tier 2 — Sub-Domain Anchor (B=1-9, C=0, DDD=000)
    Anchors a governance family to its Domain Document.
    Example: 0310000 Governance_Procedures_Anchor

  Tier 3 — Instance Document (B=1-9, C=1, DDD=001+)
    Elaborates one specific component within the sub-domain.
    Example: 0311001 Namespace_Specification

------------------------------------------------------------
4.2 PARENT CHAIN RULE
------------------------------------------------------------

Every document must have an unbroken parent chain:

  Instance → Sub-Domain Anchor → Domain Document

The parent chain must be explicitly declared in every
document's Section 2 (Parent Chain). No document may be
committed to GitHub without its complete parent chain
already committed.

============================================================
5. CODE FILE DOCUMENT CONTROL HEADER FORMAT
============================================================

Code files and operational scripts in TothBot_V2-Code are
controlled documents. They use the same 7-field header as
TothBot_V2-Docs documents, embedded as a comment block at
the top of the file.

------------------------------------------------------------
5.1 MANDATORY CODE FILE HEADER FORMAT
------------------------------------------------------------

For Python files (.py) and bash scripts (.sh):

  # DocDCN:     [DCN of governing AA=10 coding spec]
  # DocTitle:   [Descriptive_File_Title_Underscores]
  # DocVersion: [dv/rv/v + major_minor, e.g., dv1_0]
  # DocOwner:   [Bill]
  # DocPath:    [github.com/TothBot/TothBot_V2-Code/path]
  # DocDate:    [MM-DD-YYYY]
  # DocTime:    [HH:MM:SS UTC, 24-hour]

For non-Python files (.ini, .txt, .service, etc.):
  Use the file's native comment syntax.
  If no comment syntax exists, place the header as the
  first 7 lines of the file in the same key: value format.

The header MUST appear on the first lines of every file.
For bash scripts, the header follows the shebang line
(#!/usr/bin/env bash) — the shebang MUST be line 1.
No code, imports, or content may precede the header block
(beyond the shebang for bash scripts).

------------------------------------------------------------
5.2 CODE FILE HEADER FIELD RULES
------------------------------------------------------------

DocDCN:
  References the governing AA=10 coding specification DCN.
  For files governed by Engineering_Best_Practices: 1011001.
  For component .py files: the DCN of their governing spec
  (e.g., ws_manager.py → 1011002 WS_Manager_Coding_Spec).
  For operational scripts: the DCN of their governing
  operational-scripts coding spec (TBD in AA=10
  Sub-Domain 1 Operations).

DocTitle:
  Descriptive title of the file or configuration.
  Underscores only. No spaces.
  Example: Git_Ignore_Configuration, WS_Manager,
  TothBot_Sweep_Script.

DocPath:
  Full path in the repository:
  github.com/TothBot/TothBot_V2-Code/[relative/path/file]
  NOT the VPS deploy path. NOT the local Windows path.

DocVersion:
  Follows the same rules as all controlled documents.
  Starts at dv1_0. Increments on every change.

------------------------------------------------------------
5.3 CODE FILE REVISION HISTORY REQUIREMENT
------------------------------------------------------------

Every code file MUST include a Revision History section
in comment format. Format:

  # ============================================================
  # REVISION HISTORY
  # ============================================================
  #
  #   dv1_0  MM-DD-YYYY  Initial. [brief summary]
  #   dv1_1  MM-DD-YYYY  [what changed]

The Revision History grows with the file. No entry is
ever removed. The complete change history is retained.

------------------------------------------------------------
5.4 GOVERNING RULE DOCUMENT
------------------------------------------------------------

The detailed coding standards for code file headers are
defined in 1011001 Engineering_Best_Practices
(BP-HDR-001 through BP-HDR-006). Section 5 of this
document defines the namespace-level rules. 1011001
defines the implementation-level rules. Both apply.

============================================================
6. DOMAIN ASSIGNMENTS
============================================================

  AA=00  Ethics_and_Safety
  AA=01  Management
  AA=02  Operations
  AA=03  Governance
  AA=04  Exchange_Interface
  AA=05  System_Architecture
  AA=06  Risk
  AA=07  Strategy
  AA=08  Trading_System
  AA=09  Plans
  AA=10  Coding_Specification
  AA=11  Compliance

============================================================
7. FOLDER STRUCTURE — TothBot_V2-Docs
============================================================

  00XXXXX_Ethics_and_Safety/
  01XXXXX_Management/
  02XXXXX_Operations/
  03XXXXX_Governance/
  04XXXXX_Exchange_Interface/
  05XXXXX_System_Architecture/
  06XXXXX_Risk/
  07XXXXX_Strategy/
  08XXXXX_Trading_System/
  09XXXXX_Plans/
  10XXXXX_Coding_Specification/
  11XXXXX_Compliance/
  _Archive/

============================================================
8. DOCUMENT HEADER FORMAT — TothBot_V2-Docs FILES
============================================================

All controlled documents in TothBot_V2-Docs MUST begin
with the 7-field header in exactly this order:

  DocDCN:     [7-digit DCN, format AABCDDD, no spaces]
  DocTitle:   [Title_With_Underscores_No_Spaces]
  DocVersion: [dv/rv/v/rt + version, e.g. dv1_0]
  DocOwner:   [Bill]
  DocPath:    [GitHub.com/TothBot/TothBot_V2-Docs/folder/file]
  DocDate:    [MM-DD-YYYY]
  DocTime:    [HH:MM:SS UTC, 24-hour]

All seven fields are mandatory. Order is mandatory.
No field may be omitted or reordered.

DocDate and DocTime are auto-filled at every version
update in UTC. They reflect the moment of version
creation, not the moment of GitHub commit.

============================================================
9. LIFECYCLE STATES
============================================================

Every document exists in exactly one lifecycle state.
The state is encoded in DocVersion.

  dv  — Draft Version
        Active development. Under constant review. Bill may
        approve directly (dv → v). Not yet authoritative.
        Example: dv1_0, dv1_1, dv2_0

  rv  — Revision Version
        An approved document requiring changes returns to
        rv state. Not used for initial document development.
        Bill re-approves after revision is complete.
        Example: rv1_0

  v   — Approved Version
        Bill-approved. Authoritative. Governs the system.
        Changes require v → rv before returning to v.
        Example: v1_0, v2_0

  rt  — Retired
        No longer authoritative. Moved to _Archive/.
        DCN permanently reserved. Never reused.
        Example: rt1_0

State transitions:
  dv → v   (Bill approves working draft directly)
  v  → rv  (approved document requires changes)
  rv → v   (Bill re-approves after revision)
  v  → rt  (document retired)

============================================================
10. VERSIONING
============================================================

Version numbers follow the pattern: state + major_minor

  dv1_0   First draft, first minor version
  dv1_1   First draft, second minor version
  rv1_0   Review of first major version
  v1_0    First approved version
  v2_0    Second approved version (major revision)

Minor version: corrections, clarifications, additions
that do not change scope or governing decisions.

Major version: changes to scope, governing decisions,
structural changes, or any change requiring full
re-approval.

Every change increments the version. No exceptions.

============================================================
11. DCN ASSIGNMENT RULES
============================================================

11.1 Assignment Authority
Only Bill may assign DCNs. DCNs are registered in this
document (Section 12) at the time of assignment. A
document cannot exist without a registered DCN.

11.2 Assignment Sequence
Within any sub-domain, Instance Document DCNs are
assigned sequentially from DDD=001. The next available
DDD is the only valid choice. DCNs are not pre-reserved.

11.3 Permanent Reservation
Once assigned, a DCN is permanently associated with that
document title. Retired document DCNs are permanently
reserved and may never be reassigned.

============================================================
12. DCN REGISTER
============================================================

All assigned DCNs are recorded here. A DCN not in this
register is not a valid TothBot V2 DCN.

DCNs shown with current status and governing document
version as of 04-23-2026.

------------------------------------------------------------
DOMAIN DOCUMENTS (Tier 1)
------------------------------------------------------------

  0000000  Ethics_and_Safety                       v1_0
  0100000  Management                              v1_0
  0200000  Operations                              v1_0
  0300000  Governance                              v1_0
  0400000  Exchange_Interface                     dv1_0
  0500000  System_Architecture_Overview          dv1_19
  0600000  Risk                                   dv1_0
  0700000  Strategy                               dv1_0
  0800000  Trading_System                         dv1_0
  0900000  Plans                                  dv1_0
  1000000  Coding_Specification                   dv1_0
  1100000  Compliance                              [TBD]

------------------------------------------------------------
GOVERNANCE (AA=03)
------------------------------------------------------------

  0310000  Governance_Procedures_Anchor            v1_0
  0311001  Namespace_Specification                 v1_3
  0311002  Document_Creation_Workflow              v1_3
  0311003  Versioning_Procedure                    v1_1
  0311004  Document_Creation_and_Approval          v1_2
  0311005  System_Definitions_and_Terminology      v1_0

------------------------------------------------------------
OPERATIONS (AA=02)
------------------------------------------------------------

  0210000  Operations_CIATS_Anchor                dv1_0
  0211001  CIATS_Specification                    dv1_0
  0211002  Operations_Infrastructure_Specification dv1_0
  0211003  Startup_Shutdown_Procedure             dv1_0
  0211004  Incident_Response_Procedure            dv1_0
  0211005  Paper_Trading_Operations_Procedure     dv1_0

------------------------------------------------------------
EXCHANGE INTERFACE (AA=04)
------------------------------------------------------------

  0410000  Exchange_Interface_Kraken_Anchor       dv1_0
  0411001  Kraken_WS_v2_Specification            dv1_11
  0411002  Kraken_REST_API_Specification          dv1_1
  0411003  Kraken_Fee_Rate_Limit_Specification    dv1_0

------------------------------------------------------------
SYSTEM ARCHITECTURE (AA=05)
------------------------------------------------------------

  0510000  System_Components_Anchor               dv1_0
  0511001  WS_Manager_Specification               dv1_1
  0511002  Signal_Pipeline_Specification          dv1_0
  0511003  Execution_Engine_Specification         dv1_0
  0511004  Exit_Controller_Specification          dv1_2
  0511005  Position_Mirror_Specification          dv1_0
  0511006  Logger_Specification                   dv1_1

------------------------------------------------------------
RISK (AA=06)
------------------------------------------------------------

  0610000  Risk_Rules_Anchor                      dv1_0
  0611001  Position_Sizing_Rules                  dv1_0
  0611002  Drawdown_and_Halt_Rules                dv1_0
  0611003  Concentration_and_Exposure_Rules       dv1_0
  0611004  Safety_Parameter_Framework             dv1_0

------------------------------------------------------------
STRATEGY (AA=07)
------------------------------------------------------------

  0710000  Strategy_Signals_Anchor                dv1_0
  0711001  SSS_Signal_Model_Specification         dv1_0
  0711002  Regime_Taxonomy_Specification          dv1_0
  0711003  Asset_Selection_Specification          dv1_1

------------------------------------------------------------
TRADING SYSTEM (AA=08)
------------------------------------------------------------

  0810000  Trading_System_Modules_Anchor          dv1_0
  0811001  Long_Module_Specification              dv1_0
  0811002  Selection_Controller_Specification     dv1_0
  0811003  Exit_Controller_Specification          dv1_0
  0811004  Entry_Limit_Placement_Specification    dv1_0

------------------------------------------------------------
PLANS (AA=09)
------------------------------------------------------------

  0910000  Master_Plan_Anchor                     dv1_0
  0911001  TothBot_V2_Master_Plan                 dv1_0
  0920000  Business_Plan_Anchor                   dv1_0
  0921001  TothBot_V2_Business_Plan               dv1_0

------------------------------------------------------------
CODING SPECIFICATION (AA=10)
------------------------------------------------------------

  1010000  Trading_System_Coding_Anchor          dv1_12
  1011001  Engineering_Best_Practices             dv1_7
  1011002  WS_Manager_Coding_Spec                dv1_19
  1011003  Signal_Pipeline_Coding_Spec            dv1_0
  1011004  Execution_Engine_Coding_Spec            [TBD]
  1011005  Exit_Controller_Coding_Spec            dv1_2
  1011006  Position_Mirror_Coding_Spec            dv1_1
  1011007  Logger_Coding_Spec                     dv1_4
  1011008  Long_Module_Coding_Spec                dv1_0
  1011009  Selection_Controller_Coding_Spec       dv1_0
  1011010  CIATS_Coding_Spec                      dv1_6
  1011011  Risk_Engine_Coding_Spec                dv1_3
  1011012  Regime_Engine_Coding_Spec              dv1_4
  1011013  VPS_Deployment_Coding_Spec             dv1_4
  1011014  Startup_Sequence_Coding_Spec           dv1_7
  1020000  Testing_Coding_Anchor                  dv1_0
  1021001  Unit_Test_Specification                dv1_0
  1021002  Integration_Test_Specification         dv1_0
  1021003  Paper_Trading_Test_Specification       dv1_0
  1030000  GitHub_Automation_Coding_Anchor        dv1_0
  1031001  GitHub_Automation_Coding_Spec          [TBD]
  1031002  Document_Control_Automation_Test_Spec  [TBD]
  1031003  Approval_Workflow_Coding_Spec          [TBD]

Note: 1011004 Execution_Engine_Coding_Spec is marked
[TBD]. Prior register entries listed dv1_0 but no such
file exists in the project folder. A physical file is
required before the entry changes from [TBD].

============================================================
13. VALIDATION RULES
============================================================

V-001  DocDCN must be exactly 7 digits, format AABCDDD.
V-002  DocTitle must use underscores, no spaces.
V-003  DocVersion must match format: [state][MAJOR]_[MINOR].
V-004  DocOwner must be "Bill".
V-005  DocPath must match actual file location in repo.
V-006  DocDate must be MM-DD-YYYY format.
V-007  DocTime must be HH:MM:SS UTC, 24-hour format.
V-008  All seven fields are mandatory. None may be omitted.
V-009  Field order is mandatory. No reordering permitted.
V-010  DocDCN must be registered in Section 12.
V-011  Domain Document must exist before Sub-Domain Anchor.
V-012  Sub-Domain Anchor must exist before Instance Document.
V-013  No document may be committed without complete
       parent chain committed to GitHub.
V-014  DocVersion must increment on every change.
V-015  DocDate/DocTime must update on every change.
V-016  DocPath must reference TothBot_V2-Docs for
       specification documents.
V-017  DocPath must reference TothBot_V2-Code for
       code, operational script, and configuration files.
V-018  Version state must be one of: dv, rv, v, rt.
V-019  DCN AABCDDD where B=0, C=0, DDD=000 = Domain Doc.
V-020  Diagram asset filename begins with parent DCN.
V-021  Diagram asset uses .svg extension.
V-022  Diagram asset lives in same folder as parent doc.
V-023  Every diagram reference has a corresponding SVG file.
V-024  Diagram N+1 does not exist without Diagram N.
V-025  All files in TothBot_V2-Code must have a
       7-field DC header in comment format. (HR-DC-002)
V-026  All files in TothBot_V2-Code must be registered
       in 1010000 Section 5 Code File Registry. (HR-DC-003)
V-027  All files in TothBot_V2-Code must have a Revision
       History section in comment format. (HR-DC-004)

============================================================
14. FILENAME CONVENTION
============================================================

All controlled documents in TothBot_V2-Docs use this
filename format:

  [DCN]_[DocTitle]_[DocVersion].[ext]

Example:
  0311001_Namespace_Specification_v1_0.txt
  0311001_Namespace_Specification_rv1_0.txt

For code files and operational scripts in TothBot_V2-Code:
  Standard filenames per Python and system conventions.
  No version in the filename. Version is in the header.
  Git history provides version tracking.

============================================================
15. EMBEDDED ASSET ARCHITECTURE
============================================================

------------------------------------------------------------
15.1 Purpose and Scope
------------------------------------------------------------

Section 15 governs every embedded asset in every
controlled document in TothBot V2. An embedded asset is
any non-prose information element that the controlled
document presents as a unit: diagrams, images, charts,
tables rendered as images, embedded data files, audio
samples, video samples, or any other functionally-binary
content.

Section 15 defines:

  (a) The architecture by which embedded assets are
      stored, named, and referenced.
  (b) The reference syntax controlled documents use to
      cite assets.
  (c) The validation rules that detect missing or
      misnamed assets.
  (d) The rendering contract that derived formats
      (.docx, .pdf, .xlsx, .pptx) MUST honor when
      transforming the .txt source plus assets into a
      deliverable binary.
  (e) The migration rule for any existing document that
      contains inline embedded assets in violation of
      the external-file architecture.

Section 15 is the AUTHORING-LAYER governance for
embedded assets. The DELIVERY-LAYER governance lives in
TB00000 §4.1 (DR-007 Binary Artifact Content-
Completeness Validation, CI-002 External Asset
Architecture, CI-003 No Inline Binary Embeds, CI-004
Reference Syntax Binding, CI-009 Asset Presence
Verification). Sections 15 and TB00000 §4.1 work
together: Section 15 defines the asset model, TB00000
§4.1 enforces it at delivery.

Section 15 closes ERROR-TB00149-02 SVG-DROP-IN-BINARY-
GENERATION at the authoring-architecture layer.

------------------------------------------------------------
15.2 Foundational Principle — External Asset Files
------------------------------------------------------------

Every embedded asset is an EXTERNAL FILE stored
separately from the .txt controlled document that
references it. Inline embeds of any kind are PROHIBITED
in .txt source. This prohibition applies regardless of
whether the embedded content is structurally text (e.g.,
SVG XML) or structurally binary (e.g., base64 PNG).

The four reasons:

  (1) The .txt controlled document is the
      source-of-truth for prose, structure, and
      references. Mixing prose with binary-functioning
      content makes the .txt file harder to read,
      review, edit, and audit.

  (2) The rendering pipeline (which produces .docx and
      .pdf from .txt) operates deterministically when
      assets are external. Rendering pipelines that
      attempt to interpret inline embeds silently fail
      (the TB00149 ERROR-TB00149-02 origin pattern):
      the pipeline strips the inline embed and
      substitutes a stub note rather than rendering
      the asset.

  (3) Asset reuse across documents requires assets to
      be addressable. Inline embeds are not
      addressable.

  (4) Version control of assets requires assets to be
      first-class files. Inline embeds inherit the
      version of their host document and cannot be
      independently versioned.

------------------------------------------------------------
15.3 Asset Type Taxonomy
------------------------------------------------------------

Section 15 covers the following asset types. Each type
has a designated file extension and a role in the
rendering pipeline.

  TYPE      EXTENSION   ROLE
  ----      ---------   ----
  Diagram   .svg        Vector graphics for
                        architectural diagrams,
                        flowcharts, state machines,
                        timing diagrams. Source-of-
                        truth is the SVG XML; the
                        rendering pipeline rasterizes
                        to PNG for .docx embed and
                        embeds vector or raster in
                        .pdf.
  Image     .png        Raster graphics for
                        screenshots, photographs,
                        captured data plots. .png is
                        the lossless raster format;
                        rendering pipeline embeds
                        directly with no
                        transformation.
  Image     .jpg/.jpeg  Raster graphics for
                        photographs where lossy
                        compression is acceptable.
                        Rendering pipeline embeds
                        directly.
  Data      .csv        Structured data referenced by
                        the controlled document.
                        Rendering pipeline either
                        embeds as a typographic table
                        or includes the .csv as an
                        attachment.
  Audio     .wav/.mp3   Audio samples. Rendering
                        pipeline includes as
                        attachment in .docx and .pdf.
  Video     .mp4        Video samples. Rendering
                        pipeline includes as
                        attachment in .docx and .pdf.

New asset types may be added to this taxonomy only by
amendment to Section 15 with a new revision history
entry. Use of any asset type not enumerated above is
PROHIBITED.

------------------------------------------------------------
15.4 Asset Storage Location
------------------------------------------------------------

Every asset is stored in the SAME folder as its parent
controlled document in TothBot_V2-Docs.

  Example folder layout for 0500000:

    05XXXXX_System_Architecture/
      0500000_System_Architecture_Overview_dv1_22_Claude_Source.txt
      0500000_System_Architecture_Overview_dv1_22.docx
      0500000_System_Architecture_Overview_dv1_22.pdf
      0500000_Diagram1_Kraken_API_Connection_Architecture.svg
      0500000_Diagram2_Trading_Pipeline.svg
      0500000_Diagram3_Exit_Architecture.svg
      0500000_Diagram4_Regime_Taxonomy.svg
      0500000_Diagram5_CIATS_Architecture.svg
      0500000_Diagram6_Complete_System.svg

Subfolders for assets are PROHIBITED. All assets for a
given parent DCN live at the same flat level as the
parent .txt file. This keeps the folder browse-friendly
and the rendering pipeline's relative-path resolution
simple.

------------------------------------------------------------
15.5 Asset Filename Convention
------------------------------------------------------------

The canonical asset filename pattern is:

  [ParentDCN]_[Type][N]_[ShortTitle].[ext]

Where:

  ParentDCN    The 7-digit DCN of the parent
               controlled document (e.g., 0500000).
  Type         The asset type word, capitalized:
                 Diagram   for .svg vector graphics
                 Image     for .png / .jpg / .jpeg
                 Data      for .csv
                 Audio     for .wav / .mp3
                 Video     for .mp4
  N            The sequence number within the parent
               document, starting at 1, incrementing
               by 1 for each next asset of the same
               Type.
  ShortTitle   A short descriptive title in
               PascalCase or Snake_Case. Spaces
               PROHIBITED. Special characters
               PROHIBITED. Maximum 60 characters.
  ext          The file extension (lowercase) per
               §15.3 taxonomy.

Examples (canonical):

  0500000_Diagram1_Kraken_API_Connection_Architecture.svg
  0500000_Diagram2_Trading_Pipeline.svg
  1011002_Image1_WS_Manager_State_Machine.png
  0211001_Data1_CIATS_Trade_Outcome_Schema.csv

Examples (PROHIBITED — V-021 / V-025 violations):

  diagram1.svg
    (no parent DCN prefix)
  0500000_diagram1.svg
    (Type word not capitalized)
  0500000_Diagram_1.svg
    (sequence number separated by underscore)
  0500000_Diagram1.svg
    (ShortTitle missing)
  0500000_Diagram1_kraken_api connection.svg
    (spaces in ShortTitle)
  0500000_Diagram1_Kraken-API.svg
    (hyphens in ShortTitle)

A controlled document that references an asset that
does not exist as a file in the parent folder is
incomplete (V-023 violation).

------------------------------------------------------------
15.6 Reference Syntax in Controlled Documents
------------------------------------------------------------

Within a .txt controlled document, every embedded asset
is referenced using the canonical syntax:

  [Asset file: <filename>]

Where <filename> is the EXACT canonical filename of the
asset per §15.5. The reference is positioned at the
insertion point where the rendered asset should appear
in derived formats.

Examples (canonical):

  [Asset file: 0500000_Diagram1_Kraken_API_Connection_Architecture.svg]
  [Asset file: 1011002_Image1_WS_Manager_State_Machine.png]
  [Asset file: 0211001_Data1_CIATS_Trade_Outcome_Schema.csv]

LEGACY COMPATIBILITY: The pre-v1_4 reference syntax
"[Diagram file: <filename>]" is recognized by the
rendering pipeline as an alias for "[Asset file:
<filename>]" but is DEPRECATED. New references MUST use
"[Asset file: ...]". Existing "[Diagram file: ...]"
references in the corpus migrate to "[Asset file: ...]"
on the next normal version increment of each affected
document.

PROHIBITED VARIANT FORMS:

  [Image: filename.png]         (missing "file:" word)
  [File: filename.svg]          (missing asset type)
  [Diagram1]                    (missing filename)
  [See diagram 0500000_...svg]  (prose; not a formal
                                 reference)
  inline `<svg>...</svg>` XML   (CI-003 violation;
                                 see §15.10)
  base64-encoded image data     (CI-003 violation)

The rendering pipeline recognizes ONLY the canonical
syntax (and the deprecated [Diagram file: ...] alias
during migration). It does NOT scan prose for asset
mentions. A document that intends to render an asset
MUST use the formal reference syntax.

------------------------------------------------------------
15.7 Validation Rules for Embedded Assets
------------------------------------------------------------

  V-020  Asset filename begins with parent DCN
         (7 digits, no separator before underscore).

  V-021  Asset filename uses an extension enumerated
         in §15.3 (.svg, .png, .jpg, .jpeg, .csv,
         .wav, .mp3, .mp4). Other extensions
         PROHIBITED.

  V-022  Asset file lives in the SAME folder as
         parent .txt controlled document. Subfolders
         for assets PROHIBITED.

  V-023  Every "[Asset file: filename]" reference
         (or deprecated "[Diagram file: filename]"
         alias) in a .txt controlled document has a
         corresponding asset file in the parent
         folder with the EXACT canonical filename
         specified.

  V-024  Asset N+1 of a given Type does not exist
         without Asset N of the same Type for the
         same parent DCN. Sequence numbers within a
         Type are dense (no gaps).

  V-025  Asset filename matches the canonical
         §15.5 pattern. Filenames not matching are
         a V-025 defect requiring rename.

  V-026  No asset file exists in a parent folder
         without a corresponding "[Asset file: ...]"
         reference in the parent .txt controlled
         document. Orphan asset files (asset present,
         no reference) are a V-026 defect requiring
         either reference addition or asset removal.

  V-027  No "[Asset file: ...]" reference exists in
         a parent .txt controlled document without a
         corresponding asset file in the parent
         folder. Orphan references (reference
         present, no asset) are a V-027 defect — the
         inverse of V-023; both phrasings MUST hold.

  V-028  No INLINE embedded assets in any .txt
         controlled document. Inline `<svg>` XML,
         inline base64 data, inline Office Open XML
         fragments, inline PDF byte sequences — all
         PROHIBITED. Per CI-003 in TB00000 §4.1.

  V-029  Asset filename ShortTitle component
         contains only [A-Z][a-z][0-9_] characters.
         No spaces, no hyphens, no special
         characters, no Unicode outside ASCII.

  V-030  Maximum asset filename length 200
         characters (cross-platform filesystem
         portability).

------------------------------------------------------------
15.8 Rendering Contract for Derived Formats
------------------------------------------------------------

Every derived format (.docx, .pdf, .xlsx, .pptx) is
produced by a deterministic rendering pipeline that
reads the .txt Claude Source and the asset files it
references, then produces the derived format. The
pipeline MUST honor the following contract:

  RC-1 EVERY ASSET REFERENCE PRODUCES A RENDERED
       ASSET.
       For every "[Asset file: filename]" reference
       in the .txt source, the rendering pipeline
       produces a rendered asset embedded in the
       derived format at the corresponding position.
       The rendering pipeline MUST NOT silently
       drop, skip, or stub-substitute any asset
       reference.

  RC-2 RENDERING PER ASSET TYPE.
       The rendering pipeline applies the following
       per-Type transformations:

         .svg → .docx: rasterize to PNG at 2x
                       display resolution (minimum
                       150 DPI), embed PNG in body
                       at reference position.
         .svg → .pdf:  embed vector SVG directly
                       where possible; fall back to
                       PNG raster (300 DPI) where
                       vector embed unsupported.
         .png/.jpg/.jpeg → .docx, .pdf: embed
                       directly.
         .csv → .docx: render as typographic table
                       OR embed as attachment.
         .csv → .pdf:  render as typographic table
                       OR embed as attachment.
         .wav/.mp3/.mp4 → .docx, .pdf: embed as
                       file attachment.

  RC-3 PIPELINE FAILURE IS A DEFECT.
       If the rendering pipeline cannot render any
       asset reference, the pipeline MUST FAIL and
       the derived format MUST NOT be produced. A
       failed render is reported to Claude (and
       through Claude to Bill) as a defect requiring
       remediation. Stub-note substitution is
       PROHIBITED per CI-008.

  RC-4 NO PIPELINE-INTRODUCED CONTENT.
       The rendering pipeline produces ONLY content
       derived from the source-of-truth set. It MUST
       NOT add prose, captions, watermarks,
       footers, or any other content not present in
       the source-of-truth. Header / footer / page
       numbers may be added per a documented
       rendering profile, but no body content.

  RC-5 NO PIPELINE-INTRODUCED LOSS.
       The rendering pipeline preserves ALL prose,
       structure, references, and asset embeds from
       the source-of-truth set, with only the
       transformations enumerated in TB00000 §4.1
       CI-007 (T-1 through T-5) permitted. Any
       deviation is a defect.

  RC-6 DETERMINISTIC OUTPUT.
       Two invocations of the rendering pipeline on
       the same source-of-truth set produce
       semantically equivalent output. Byte-level
       differences are permitted only in non-content
       metadata (e.g., creation timestamps in PDF
       metadata).

------------------------------------------------------------
15.9 Migration Rule for Existing Inline Embeds
------------------------------------------------------------

Any existing controlled document that contains inline
embedded assets (in violation of V-028 / CI-003) MUST
be migrated to the external-file architecture on the
next normal version increment of that document. The
migration procedure is:

  STEP 1 EXTRACTION.
         Each inline `<svg>` XML embed is extracted
         from the .txt source and saved as a
         separate file in the parent folder, named
         per §15.5 canonical convention. The
         extraction preserves the SVG XML byte-for-
         byte; no editing is performed.

  STEP 2 REFERENCE SUBSTITUTION.
         Each inline embed in the .txt source is
         replaced by the canonical "[Asset file:
         filename]" reference at the same position.

  STEP 3 RENDERING REGENERATION.
         The .docx and .pdf are regenerated from the
         migrated .txt source plus the extracted
         asset files via the §15.8 rendering
         pipeline. The regenerated binaries pass
         TB00000 §4.1 DR-007 content-completeness
         validation.

  STEP 4 REVISION HISTORY ENTRY.
         The version increment's revision history
         entry explicitly enumerates the migration:
           - Asset extraction count and filenames.
           - Reference substitution count.
           - Binary regeneration confirmation.

  STEP 5 VALIDATION.
         Run V-020 through V-030 against the
         migrated document and asset files. All
         MUST pass.

Migration is NOT a content change in the
information-content sense (the same diagrams are
present after migration, just stored differently).
However, the structural change (.txt source layout,
asset file presence, reference syntax) is significant
and MUST be documented in the revision history.

------------------------------------------------------------
15.10 Prohibited Patterns
------------------------------------------------------------

The following patterns are PROHIBITED in any
controlled document. Detection of any pattern is a
V-028 / CI-003 violation requiring immediate
remediation per §15.9.

  PP-1  Inline `<svg>...</svg>` XML embed in .txt
        source. (THE TB00149 ERROR-TB00149-02
        ORIGIN PATTERN.)

  PP-2  Inline base64-encoded image data in .txt
        source.

  PP-3  Inline Office Open XML fragments
        (`<w:p>`, `<w:tbl>`, etc.) in .txt source.

  PP-4  Inline PDF byte sequences in .txt source.

  PP-5  Embedded binary content via any encoding
        scheme (uuencode, hex, ASCII85) in .txt
        source.

  PP-6  Asset files stored in subfolders below the
        parent .txt source location.

  PP-7  Asset filenames not conforming to §15.5
        canonical pattern.

  PP-8  Reference syntax variant forms not
        recognized in §15.6.

  PP-9  Asset files present without corresponding
        references (orphan assets, V-026
        violation).

  PP-10 References present without corresponding
        asset files (orphan references, V-027
        violation).

============================================================
16. RELATED DOCUMENTS
============================================================

  0300000  Governance           (parent Domain Document)
  0310000  Governance_Procedures_Anchor  (parent Anchor)
  0311002  Document_Creation_Workflow
  0311003  Versioning_Procedure
  0311004  Document_Creation_and_Approval_Procedure
  1011001  Engineering_Best_Practices (BP-HDR code file rules)
  TB00000  Project Instructions for Claude (§4.2 ABSOLUTE
           STANDING RULE; §11 authoritative path tree)

============================================================
17. REVISION HISTORY
============================================================

  v1_4   05-04-2026  ERROR-TB00149-02 REMEDIATION —
                     FULL REWRITE OF SECTION 15.

                     Section 15 retitled "EMBEDDED ASSET
                     ARCHITECTURE" (was "DIAGRAM ASSET
                     SPECIFICATION") and rewritten in
                     full to govern every embedded asset
                     type (not just SVG diagrams) and
                     to define the complete authoring,
                     storage, reference, validation, and
                     rendering-contract architecture.

                     NEW STRUCTURE:
                       15.1 Purpose and Scope
                       15.2 Foundational Principle —
                            External Asset Files
                       15.3 Asset Type Taxonomy
                            (Diagram .svg, Image .png/
                            .jpg/.jpeg, Data .csv,
                            Audio .wav/.mp3, Video
                            .mp4)
                       15.4 Asset Storage Location
                            (same folder as parent;
                            subfolders prohibited)
                       15.5 Asset Filename Convention
                            ([ParentDCN]_[Type][N]_
                            [ShortTitle].[ext]; with
                            canonical examples and
                            prohibited variants)
                       15.6 Reference Syntax in
                            Controlled Documents
                            ([Asset file: filename]
                            with [Diagram file: ...]
                            deprecated alias for
                            backward compatibility)
                       15.7 Validation Rules
                            (V-020 through V-030)
                       15.8 Rendering Contract for
                            Derived Formats
                            (RC-1 through RC-6;
                            per-type rendering rules
                            for .svg/.png/.jpg/.csv/
                            audio/video → .docx/.pdf)
                       15.9 Migration Rule for
                            Existing Inline Embeds
                            (5-step procedure)
                       15.10 Prohibited Patterns
                             (PP-1 through PP-10)

                     REASON FOR CHANGE:
                     ERROR-TB00149-02 SVG-DROP-IN-
                     BINARY-GENERATION (TB00149
                     origin). Prior §15 ("DIAGRAM ASSET
                     SPECIFICATION") was scoped to
                     diagram-type assets only and lacked
                     a rendering contract for derived
                     formats. The 0500000 dv1_22 .txt
                     source produced in TB00149
                     contained 6 inline `<svg>` XML
                     embeds in violation of §15.4
                     reference syntax (which mandates
                     external SVG files), and the
                     rendering pipeline silently
                     stripped the inline embeds and
                     substituted a stub note in the
                     generated .docx and .pdf.
                     Investigation revealed: (a) prior
                     §15 said diagrams MUST be external
                     files but did not say inline embeds
                     are PROHIBITED; (b) prior §15 had
                     no rendering contract requiring the
                     pipeline to honor every reference;
                     (c) prior §15 covered only
                     diagrams, leaving image/data/
                     audio/video assets ungoverned; (d)
                     prior §15 had no migration
                     procedure for existing non-
                     compliant documents; (e) prior §15
                     had no enumeration of prohibited
                     patterns. The dv1_22 .docx and .pdf
                     diagram-loss defect is one
                     instantiation of an
                     architectural-governance gap that
                     extended across all asset types and
                     rendering pipelines.

                     Bill's directive (TB00149 chat):
                     "Rewrite the entire section to be
                     CLEAR, UNAMBIGUOUS, AND CORRECT.
                     Do not just add a patch. Redo the
                     entire section in ALL REQUIRED
                     DOCUMENTS to ensure this does not
                     continue to happen. Make sure it
                     is not only this situation, but
                     make sure NO INFORMATION is ever
                     lost by accident or drift."

                     FP/DP ANALYSIS:
                     Option A (add new sub-sections to
                     existing §15) — REJECTED per Bill
                     explicit directive against patch-
                     only approach.
                     Option B (full §15 rewrite covering
                     all asset types, formal reference
                     syntax with prohibition of
                     variants, complete validation rule
                     set V-020 through V-030, rendering
                     contract RC-1 through RC-6,
                     migration procedure, enumerated
                     prohibited patterns) — ACCEPTED.
                     Aligned with TB00000 v2_19 §4.1
                     full rewrite (companion increment
                     same session). The two together
                     form the authoring + delivery
                     governance for the embedded-asset
                     architecture.

                     Decision: Option B.

                     PARENT CHAIN COMPLIANCE:
                     0311001 is an instance document
                     under 0310000 Governance_
                     Procedures_Anchor under 0300000
                     Governance Domain Document.
                     Parent chain unchanged. Section 17
                     Revision History updated. No
                     changes to other §s of 0311001.

                     COMPANION DELIVERABLES THIS
                     SESSION (TB00149 v1_1 batch):
                     TB00000 v2_18 -> v2_19 (full
                       rewrite of §4.1; companion
                       delivery + integrity governance).
                     0311002 v1_8 -> v1_9 (DC-
                       PREFLIGHT-007 asset inventory
                       check + DC-PREFLIGHT-008
                       binary-in-folder content-
                       completeness check added to
                       STEP A §8.2.5).
                     TB00149 Session Log v1_0 -> v1_1
                       (supersedes; documents this
                       governance addition).
                     TB00149 Next Session Instructions
                       v1_0 -> v1_1 (supersedes;
                       directs TB00150 to apply new
                       governance to dv1_22 SVG
                       extraction + .docx/.pdf
                       regeneration).

                     OPEN ITEMS UPDATED:
                     OI-080 CONTENT-INTEGRITY VIOLATION
                       RECURRENCE TRACKING. Initial
                       cumulative count = 1
                       (ERROR-TB00149-02). New §15
                       authoring rules close the
                       authoring-architecture component
                       at the governance layer.
                     ERROR-TB00149-02 logged in TB00149
                       SL v1_1 §7. CLOSED at governance
                       layer via TB00000 v2_19 §4.1 +
                       0311001 v1_4 §15 + 0311002 v1_9
                       STEP A additions. Operational
                       verification at every future
                       authoring + binary delivery
                       action.

                     The 0500000 dv1_22 binary
                     remediation (extract 6 inline SVG
                     XML embeds into separate .svg
                     files per new §15.5; rewrite
                     dv1_22 .txt to use §15.6
                     reference syntax; regenerate
                     .docx/.pdf with rasterized PNG
                     embeds; validate per new TB00000
                     §4.1 DR-007) is deferred to
                     TB00150 per TB00149 NSI v1_1
                     STREAM A. The governance is in
                     place first; the application of
                     the governance to dv1_22 is
                     STREAM A of the next session.

                     Bill override: 0311001 v1_3 ->
                     v1_4 is enacted under Bill's
                     standing document-control
                     override per Bill's TB00149
                     explicit direction. Direct
                     increment v1_3 -> v1_4; no rv
                     state.

  v1_3   04-23-2026  OI-039 remediation. Four changes.

                     (1) HR-PATH-004 OPERATIONAL-SCRIPT
                     EXCEPTION deleted entirely. The
                     exception from v1_2 authorized non-
                     controlled operational scripts to be
                     staged via a now-prohibited local
                     path. Per TB00000 v2_11 §4.2
                     ABSOLUTE STANDING RULE and v2_12
                     §3.4.2/§3.4.3 clarification,
                     operational scripts are now first-
                     class controlled documents living
                     in TothBot_V2-Code operations/
                     subfolder. The exception is no
                     longer needed and its text was the
                     recurrence vector for OI-033 /
                     OI-036 / OI-039.

                     (2) HR-PATH-005 (was: exception does
                     not extend to controlled documents)
                     DELETED as redundant — with no
                     exception, nothing to scope.

                     (3) HR-PATH-006 (was: uncertain
                     file treated as controlled)
                     renumbered to HR-PATH-004 with
                     expanded text: HR-DC-001 establishes
                     that ALL files in TothBot_V2-Code
                     are controlled, so no operational
                     exception exists.

                     (4) NEW HR-PATH-005 added: cross-
                     references TB00000 v2_12 §4.2
                     ABSOLUTE STANDING RULE prohibiting
                     the retired cloud-sync path;
                     requires Claude to STOP and ask
                     Bill if a path outside the TB00000
                     §11 authoritative tree is needed.

                     (5) HR-PATH-003 scope expanded to
                     cover both controlled code files
                     AND operational scripts (both now
                     live in TothBot_V2-Code). Prohibited-
                     path example removed per TB00000
                     v2_12 §4.2 OP-003 pre-emit scan.
                     Prohibition text preserved in
                     generalized form.

                     (6) Section 2 SCOPE and Section 3
                     GOVERNING REPOSITORIES updated:
                     TothBot_V2-Code purpose statement
                     now includes operational support
                     scripts alongside trading system
                     code. Subfolder layout documented
                     (tothbot/, operations/, tests/).
                     File format list updated to include
                     .sh.

                     (7) HR-002 rewritten to include
                     operational support scripts in the
                     permitted scope of TothBot_V2-Code.
                     Document control logic remains
                     prohibited.

                     (8) Section 5 CODE FILE DOCUMENT
                     CONTROL HEADER FORMAT updated to
                     cover bash scripts alongside Python
                     files. Shebang-first rule added for
                     bash scripts. DocDCN field rules
                     expanded to include operational
                     scripts. DocTitle example added.

                     (9) V-017 updated to include
                     operational script files alongside
                     code and configuration files.

                     (10) Corrective action paragraph
                     rewritten: references both OI-033
                     (original path-governance gap) and
                     OI-039 (governance residual
                     contamination). Prohibited-string
                     reference removed per TB00000
                     v2_12 §4.2 OP-003.

                     (11) Section 12 DCN Register
                     updated: 0311001 v1_2 → v1_3,
                     0311003 v1_0 → v1_1, 1010000
                     dv1_11 → dv1_12.

                     (12) Section 16 RELATED DOCUMENTS:
                     added TB00000 cross-reference for
                     §4.2 and §11.

                     Bill override: document control
                     constraints (v → rv → v transition
                     for significant content changes) are
                     explicitly overridden per Bill's
                     directive on 04-22-2026, continuing
                     the OI-039 remediation override
                     scope. v1_2 → v1_3 is a significant
                     content change (rule deletion plus
                     restructuring) but does not pass
                     through rv state. Approved by Bill.

  v1_2   04-22-2026  Three changes.
                     (1) Section 3.1 PATH GOVERNANCE FOR
                     CONTROLLED DOCUMENTS added. HR-PATH-001
                     through HR-PATH-006 defined the two
                     governed local repository paths as the
                     sole authoritative locations for
                     controlled documents. OPERATIONAL-SCRIPT
                     EXCEPTION (HR-PATH-004) formally scoped
                     Desktop-path staging to non-controlled
                     operational scripts only (example:
                     /root/tothbot_sweep.sh). Controlled
                     code files were explicitly prohibited
                     from Desktop staging. Closed OI-033
                     (DEFECT-CLAUDE-DESKTOP-STAGING-PATH-001,
                     originating TB00112). NOTE: HR-PATH-004
                     exception text contained a prohibited-
                     string example that became the
                     recurrence vector for OI-039 — fully
                     remediated in v1_3.
                     (2) Section 12 DCN Register refreshed
                     to actual current versions across all
                     entries. Full sweep performed in TB00114
                     cold load. New sub-sections added for
                     OPERATIONS (AA=02), RISK (AA=06),
                     STRATEGY (AA=07), TRADING SYSTEM (AA=08),
                     and PLANS (AA=09) — these domains
                     previously had no register entries
                     despite active documents existing.
                     Governance register entries corrected:
                     0311001 v1_2, 0311002 v1_3, 0311004 v1_2
                     (corrected from rv states to approved v
                     per VP-004 v → v minor correction).
                     (3) Revision History backfilled with
                     v1_1 promotion entry (previously missing)
                     and this v1_2 entry added.
                     Minor correction per VP-004 (v1_1 → v1_2
                     with Bill approval; rv state not
                     required). Approved by Bill.

  v1_1   04-11-2026  Promoted from rv1_0 to v1_1. Content
                     changes were delivered under rv1_0
                     (Section 2 Scope expanded to include
                     TothBot_V2-Code; HR-DC-001 through
                     HR-DC-004 added; Section 5 Code File
                     Header format added; V-025 through
                     V-027 added; Related Documents updated
                     to include 1011001). Approved by Bill.
                     This promotion entry was omitted from
                     the Revision History at time of
                     promotion — backfilled in v1_2 per
                     TB00114 discrepancy audit.

  rv1_0  04-11-2026  Section 2 Scope expanded to include
                     TothBot_V2-Code as a governed repository.
                     Added HR-DC-001 through HR-DC-004:
                     all files in TothBot_V2-Code are
                     controlled documents requiring 7-field
                     headers, Revision History, and Code File
                     Registry entries. Section 3 updated with
                     header format note for both repos.
                     Section 5 (new) added: code file DC
                     header format, field rules, and Revision
                     History requirement. Section 7 updated:
                     renamed from "Folder Structure" to
                     clarify TothBot_V2-Docs scope. V-025,
                     V-026, V-027 added to Section 13. Related
                     Documents updated to include 1011001.
                     Corrective action from TB00079 governance
                     gap (Claude error: DC applies to code
                     files). Approved by Bill.

  v1_0   04-04-2026  Promoted from dv to v. Corrected
                     Section 9 lifecycle states and
                     transitions: dv → v valid (direct
                     promotion); rv is for revising approved
                     documents only; removed incorrect
                     dv → rv, rv → dv, v → dv paths.
                     Approved by Bill.

  dv1_3  04-04-2026  DocPath correction: TothBot-V2-Docs
                     corrected to TothBot_V2-Docs throughout.

  dv1_0  03-23-2026  Initial document. Clean-slate
                     namespace specification for TothBot
                     V2. 12-domain structure. Three-tier
                     document hierarchy. Key innovations:
                     AA=04 Exchange_Interface, AA=02
                     Operations unifying Quality/Ops/CIATS,
                     AA=01 Management (Deming), AA=08
                     Trading_System. B=0 defined as
                     categorically distinct Domain Document
                     level vs B=1-9 Sub-Domain levels.

  dv1_1  03-23-2026  Reduced from 12 domains to 11 domains.
                     AA=09 Master_Plan and AA=10 Business_Plan
                     merged into AA=09 Plans.

  dv1_2  03-24-2026  Added Section 15 Diagram Asset
                     Specification. Five validation rules
                     added (V-020 through V-024).

============================================================
END OF DOCUMENT
============================================================
