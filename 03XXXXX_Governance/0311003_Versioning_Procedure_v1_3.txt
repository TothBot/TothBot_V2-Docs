DocDCN:     0311003
DocTitle:   Versioning_Procedure
DocVersion: v1_3
DocOwner:   Bill
DocPath:    GitHub.com/TothBot/TothBot_V2-Docs/03XXXXX_Governance/0311003_Versioning_Procedure_v1_3.txt
DocDate:    06-09-2026
DocTime:    06:45:00 UTC

============================================================
VERSIONING PROCEDURE
============================================================

============================================================
1. PURPOSE
============================================================

This document defines the rules governing how TothBot V2
controlled documents are versioned. It specifies the
version format, when versions increment, how lifecycle
state changes, and how version history is maintained.

============================================================
2. PARENT CHAIN
============================================================

  0000000  Ethics_and_Safety            (Domain)
  0100000  Management                   (Domain)
  0200000  Operations                   (Domain)
  0300000  Governance                   (Domain Document)
  0310000  Governance_Procedures_Anchor (Sub-Domain Anchor)
  0311003  This document                (Instance)

============================================================
3. VERSION FORMAT
============================================================

------------------------------------------------------------
VP-001  DOCVERSION FORMAT
------------------------------------------------------------

DocVersion follows the format:  [STATE][MAJOR]_[MINOR]

  STATE:  dv (working draft)
          rv (review version)
          v  (approved)
          rt (retired)

  MAJOR:  positive integer, starts at 1
  MINOR:  positive integer, starts at 0

Examples:
  dv1_0  — working draft, first version
  rv1_0  — review version, first version
  v1_0   — approved, first version
  v1_1   — approved, second minor version
  v2_0   — approved, second major version
  rt1_0  — retired

------------------------------------------------------------
VP-002  VERSION COMPONENT DEFINITIONS
------------------------------------------------------------

MAJOR version increments for:
  - Significant content changes (new sections, major
    rewrites, architectural changes captured).
  - Lifecycle state transitions that represent a
    completed review/approval cycle (e.g., v1_X → v2_0
    on a major revision that went through rv state).

MINOR version increments for:
  - Corrections, additions, or clarifications within
    existing structure.
  - Addition of new requirements to "Must be Included"
    sections in Working Draft coding specs.
  - Header field updates (date, time) when making any
    content change.

Both MAJOR and MINOR reset to (0) when STATE changes
to a new lifecycle stage (e.g., dv1_3 → rv1_0).

------------------------------------------------------------
VP-003  EVERY CHANGE INCREMENTS VERSION
------------------------------------------------------------

Every change to any controlled document — no matter how
small — MUST increment the version. There is no such
thing as a trivial change that does not require a version
increment. If the document changed, the version changes.

DocDate and DocTime are updated on every version change
to reflect the UTC timestamp of the change.

============================================================
4. LIFECYCLE STATE TRANSITIONS
============================================================

------------------------------------------------------------
VP-004  STATE TRANSITION RULES
------------------------------------------------------------

A document in dv state is under constant review. Bill
may approve a dv document directly without requiring an
intermediate rv state.

  dv → dv:   Any change during working draft phase.
             Version increments. Claude executes.

  dv → v:    Bill approves the working draft directly.
             Version resets to v1_0.
             BILL ACTION ONLY. Claude cannot execute this.
             The rv state is not required for initial
             document development — dv is already under
             constant review.

  v → rv:    An approved document that requires changes
             returns to rv state for revision.
             BILL ACTION ONLY. Claude cannot execute this.
             Version resets to rv1_0.

  rv → rv:   Revisions made during revision of an approved
             document. Version increments (rv1_1, rv1_2).
             Claude executes per Bill feedback.

  rv → v:    Bill re-approves after rv revision.
             Version resets to v1_0.
             BILL ACTION ONLY. Claude cannot execute this.

  v → v:     Minor correction to approved document.
             Version increments (v1_1, v1_2, etc.).
             For significant content changes: must go
             through rv state (v → rv → v).
             For minor corrections (typos, clarifications
             that do not change technical content): v1_0
             → v1_1 with Bill approval; rv not required.

  v → rt:    Document retired. Superseded or no longer
             applicable. Retirement removes the document
             from HEAD via `git rm` + commit + push. Git
             history preserves all prior versions. No
             rt-prefix successor file at HEAD; no
             tombstone document body; no _Archive folder.
             Use `git log --follow <path>` to inspect a
             retired document's full version history.
             Version state is recorded in DocVersion at
             the moment of retirement (e.g., rt1_0) for
             the git-history record only.
             BILL ACTION ONLY. Claude cannot execute this.

------------------------------------------------------------
VP-005  STATE TRANSITION PROHIBITION
------------------------------------------------------------

Claude may NEVER change lifecycle state. Specifically:

  - Claude may NOT change dv → v, v → rv, rv → v, or
    v → rt. All state transitions are BILL ACTION ONLY.
  - Claude may NOT mark a document as approved.
  - Claude may NOT retire a document.

Only Bill may change lifecycle state.

Claude MAY increment versions within the same state.
Claude MAY change dv to dv with a higher version number.

============================================================
5. WORKING DRAFT EXCEPTION (WD EXCEPTION)
============================================================

------------------------------------------------------------
VP-006  WD EXCEPTION FOR CODING SPECS
------------------------------------------------------------

Working Draft (dv) coding specifications may capture
requirements before their parent specification documents
exist. This is permitted and does not violate document
control because:

  (a) The WD document contains a "Must be Included From
      Previous Analysis" section that preserves requirements.
  (b) The WD document is explicitly marked as a placeholder.
  (c) The WD document cannot reach approved (v) state
      until its parent specification is approved (v).

WD documents version normally within the dv state (dv1_0,
dv1_1, etc.) as requirements are added to them.

------------------------------------------------------------
VP-007  WD EXCEPTION DOES NOT PERMIT GITHUB COMMIT
------------------------------------------------------------

WD Exception documents may NOT be committed to GitHub
until their complete parent chain exists and is committed.
They exist in the Claude.ai project only until that
condition is met.

============================================================
6. VERSION HISTORY REQUIREMENTS
============================================================

------------------------------------------------------------
VP-008  REVISION HISTORY SECTION
------------------------------------------------------------

Every controlled document MUST include a Revision History
section. Each entry contains:

  [version]  [date]  [summary of changes]

The Revision History grows with the document. No entry
is ever removed. The complete change history is retained.

Format:
  dv1_0  MM-DD-YYYY  Initial document. [brief summary]
  dv1_1  MM-DD-YYYY  [what changed]
  rv1_0  MM-DD-YYYY  [submitted for review]
  v1_0   MM-DD-YYYY  [approved by Bill; brief summary]

VP-008 EXTERNALIZED REVISION HISTORY (added v1_3; DEC-123).
A controlled document MAY hold its Revision History -- and, for
a diagram-bearing design document, its per-figure provenance
(figcaption provenance / extension stacks, root <desc>
R-increment chains, and design-completeness attestation blocks)
-- in a dedicated companion ARCHIVE instance document under its
domain's history sub-domain, instead of inline, provided ALL of:
  (a) the externalized content is relocated VERBATIM and is
      thereafter APPEND-ONLY in the archive. Relocation is NOT
      removal -- the entry persists verbatim, so the VP-008
      "no entry is ever removed" guarantee is satisfied by the
      archive.
  (b) the live document retains a pointer to the archive; a
      diagram-bearing document additionally retains each
      figure's CURRENT R-state on the live surface.
  (c) the complete change history remains retained across the
      archive PLUS git history.
Rationale (binary test DEC-096): revision-history /
R-increment-provenance / attestation narrative is document-
control, not design content; for a diagram-bearing design
document it does not belong on the design surface. Precedent:
0500000 -> 0521001 (anchor 0520000), begun at TB00550 / TB00551
and completed under DEC-123.

------------------------------------------------------------
VP-009  VERSION IN FILENAME
------------------------------------------------------------

The DocVersion is embedded in the filename:

  [DCN]_[Title]_[DocVersion].txt

Example:
  0311003_Versioning_Procedure_dv1_0.txt
  0311003_Versioning_Procedure_v1_0.txt

The filename version must match the DocVersion in the
header at all times. When version increments, the filename
changes. Both old and new versions are retained by Bill.

------------------------------------------------------------
VP-010  SUPERSEDED DOCUMENTS
------------------------------------------------------------

When a document is updated, the prior version is handled
per the document class and the authoritative storage
tree in TB00000 §11:

  (a) For session logs and TB00000 (non-Git files):
      Prior versions are retained by Bill in the local
      session-logs folder per TB00000 §11:

        C:\Users\wstot\GitHub\TothBot\TBXXXXX_Session_Logs\

      alongside the current version. No separate archive
      location is used.

  (b) For all other controlled documents (TothBot_V2-Docs
      and TothBot_V2-Code files):
      Superseded versions are managed through git history
      in the governed local repository and on GitHub.
      No separate local archive is maintained. Use
      `git log --follow <file>` to inspect a file's
      version history.

  (c) For retired documents (rt-state) in
      TothBot_V2-Docs and TothBot_V2-Code:
      Retirement removes the document from HEAD via
      `git rm` + commit + push. Git history preserves
      all prior versions including the final v-state
      version at the moment of retirement. No
      rt-prefix successor file is authored or
      committed at HEAD; no tombstone document body;
      no _Archive folder anywhere (local, remote, or
      VPS). Use `git log --follow <path>` to inspect
      a retired document's full version history.
      Retirement removes the file from the Claude.ai
      project knowledge folder, the local C: working
      tree, AND GitHub HEAD; the document continues
      to exist as history in git only.

This standing practice is codified in TB00000 v2_32
§11 and supersedes any prior text in this document
or in 0311001 prescribing a local _Archive folder,
an rt-prefix file at HEAD, or a tombstone document
body. All version history is managed through git.

============================================================
7. VERSION VALIDATION RULES
============================================================

VP-011  DocVersion must match the version in the filename.
VP-012  DocDate must be MM-DD-YYYY format.
VP-013  DocTime must be HH:MM:SS UTC, 24-hour format.
VP-014  State may only be: dv, rv, v, rt.
VP-015  MAJOR must be >= 1.
VP-016  MINOR must be >= 0.
VP-017  Version must increment on every document change.
VP-018  DocDate/DocTime must update on every change.

These rules are enforced manually by Claude pre-GitHub
automation and automatically by Pathway 1 post-deployment.

============================================================
8. REVISION HISTORY
============================================================

  v1_3   06-09-2026  TB00635 VP-008 EXTERNALIZED REVISION
                     HISTORY PROVISION (DEC-123). Added the
                     VP-008 externalization provision: a
                     controlled document MAY hold its revision
                     history -- and a diagram document's
                     per-figure provenance -- in a dedicated
                     append-only archive instance under the
                     domain history sub-domain, with a live
                     pointer + retained current R-state, content
                     relocated VERBATIM (relocation is not
                     removal), complete history retained across
                     archive + git. Codifies and closes the
                     latent gap that TB00551 relocated the
                     0500000 Section 14 into 0521001 without a
                     VP-008 amendment. Binary test DEC-096:
                     provenance is document-control, not design
                     content. Companion to DEC-123 Phase G
                     (0311001 v1_46 + TB00000 + 0521001 dv1_1).
                     Direct v1_2 -> v1_3 minor increment
                     (clarifying provision; no technical-content
                     reversal) per VP-004 v -> v with Bill
                     approval. DocDate / DocTime refreshed.

  v1_2   05-12-2026  TB00224 TURN 2 GOVERNANCE
                     RECONCILIATION — VP-004 v → rt
                     TRANSITION + VP-010(c) REWRITE PER
                     TB00000 v2_32 §11 RETIRED-DOCUMENT
                     STANDING-PRACTICE CODIFICATION.

                     Two surgical edits removing
                     contradictions with the corrected
                     retirement standing practice
                     codified in TB00000 v2_32 §11:

                     CHANGE 1 — VP-004 v → rt transition
                     rule. Prior text (v1_1): "Document
                     retired. Superseded or no longer
                     applicable. Moved to _Archive.
                     Version state changes to rt (e.g.,
                     rt1_0). BILL ACTION ONLY." Replaced
                     with explicit retirement methodology:
                     `git rm` + commit + push; git history
                     preserves all prior versions; no
                     rt-prefix successor file at HEAD;
                     no tombstone document body; no
                     _Archive folder. `git log --follow
                     <path>` retrieves version history.
                     DocVersion still records rt-state at
                     the moment of retirement for the
                     git-history record only.

                     CHANGE 2 — VP-010(c) rewrite. Prior
                     text (v1_1): "The TothBot_V2-Docs
                     _Archive folder contains ONLY
                     documents in rt (retired) state ...
                     Only documents with rt state are
                     moved to _Archive. All other version
                     history is managed through git."
                     Replaced with the v2_32 §11 standing
                     practice: retirement = `git rm` +
                     commit + push; no _Archive folder
                     (local, remote, or VPS); no rt-prefix
                     file at HEAD; no tombstone document
                     body; retirement removes the file
                     from Claude.ai project folder, local
                     C: working tree, AND GitHub HEAD;
                     `git log --follow` retrieves version
                     history; document exists as git
                     history only.

                     REASON FOR CHANGE.
                     TB00000 v2_32 §11 (enacted 2026-05-12
                     at TB00223 close per Bill chat-turn
                     directive) codified the corrected
                     retired-document standing practice
                     and contains an explicit supersession
                     clause overriding prior text in
                     0311003 VP-010(c) and 0311001 §7 /
                     §9. Per the v2_32 §12 FP/DP Option A
                     analysis, governance reconciliation
                     of 0311003 + 0311001 was deferred to
                     TB00224 as a carry-forward (G-6 in
                     TB00223 SL §6; C-2 in TB00224 NSI §6).

                     The TB00220 SL §4.1 originating
                     Bill correction observed a multi-
                     session propagation lapse where the
                     rt-prefix file methodology was
                     authored at TB00213 (creating
                     1011011 rt1_3 and 1011004 rt1_0 at
                     GitHub HEAD) and not surfaced as a
                     defect through TB00220. Bill chat-
                     turn clarifications at TB00223
                     established the corrected standing
                     practice: GitHub git history IS the
                     version archive; the file at HEAD
                     does not need a rt-prefix rename or
                     a tombstone body. TB00224 Turn 1
                     this session executed cleanup of the
                     TB00213 rt-prefix files (commit
                     475fd59).

                     FP/DP ANALYSIS — VP-010(a) STRUCTURE.
                     TB00224 NSI §3.3(a) offered two
                     options for VP-010(c) handling:
                     Option (i) retire VP-010(c) + add
                     new VP-011 RETIREMENT METHODOLOGY
                     rule; Option (ii) rewrite VP-010(c)
                     in place + update VP-004 v → rt
                     transition; no new VP rule.

                     Option (i) — REJECTED. VP-011 is
                     already taken (line 264 v1_1:
                     "DocVersion must match the version
                     in the filename"). Adding a new
                     VP-011 RETIREMENT METHODOLOGY rule
                     requires renumbering existing
                     VP-011..VP-018 to VP-012..VP-019 —
                     a cascade of 8 renumbers with high
                     risk of breaking forward references
                     in this document and in TB00000 +
                     0311001 + 0311002 + session-log
                     citation history.

                     Option (ii) — ACCEPTED. Two surgical
                     edits (VP-004 + VP-010(c)) with no
                     renumbering and no new rule
                     creation. Minimal edit surface.
                     Zero forward-reference cascade.

                     Decision: Option (ii).

                     PARENT CHAIN COMPLIANCE.
                     0311003 parent chain: 0300000
                     Governance (Domain) → 0310000
                     Governance_Procedures_Anchor
                     (Sub-Domain Anchor) → 0311003
                     Versioning_Procedure (Instance).
                     All parent documents present in
                     project folder. No new domain or
                     sub-domain document required.

                     COMPANION DELIVERABLES THIS SESSION
                     (TB00224 SESSION-CLOSE):
                       0311001 v1_12 -> v1_13 (companion
                         amendment — §7 _Archive removal
                         + §9 rt-state definition update;
                         same root cause).
                       TB00224 Session_Log v1_0.
                       TB00225 Next_Session_Instructions
                         v1_0.
                       (Optional, pending Bill approval
                         on TB00224 chat-turn 0 audit:
                         TB00000 v2_32 -> v2_33 for
                         F-1..F-4 obsolete-citation
                         findings.)

                     OPEN ITEMS UPDATED.
                       G-6 archive-folder governance
                         gap CLOSED via this amendment
                         (companion to 0311001 v1_13).
                       C-2 NEW carry-forward CLOSED.

                     RR-SCOPE-007 SELF-AUTHORED-NSI-
                     SOURCE-PROHIBITION COMPLIANCE.
                     The scope restriction applied here
                     (Option ii instead of Option i)
                     traces to Bill chat-turn directive
                     at TB00223 chat-turn TBD (TB00000
                     v2_32 §11 enactment) — Bill verbatim:
                     "TB00000 §11 may need a brief note
                     matching the same standing practice."
                     Option selection between (i) and (ii)
                     is a derivable engineering decision
                     under RR-SCOPE-005 next-logical-step
                     execution; not a scope restriction
                     requiring Bill ratification.

                     Bill override: 0311003 v1_1 -> v1_2
                     is enacted under Bill's standing
                     document-control override continuing
                     the scope established in TB00115
                     (TB00000 v2_10 -> v2_11), TB00116
                     (v2_11 -> v2_12), TB00120 (v2_12 ->
                     v2_13). Direct increment v1_1 ->
                     v1_2; no rv state required for
                     content additions that close a
                     tracked OI / carry-forward. Per
                     Bill's TB00224 chat-turn directive
                     bundled with the OCS execution
                     directive.

  v1_1   04-23-2026  OI-039 remediation. VP-010 SUPERSEDED
                     DOCUMENTS rewritten.

                     Previously (v1_0), VP-010(a) stated
                     that prior versions were "retained by
                     Bill on [prohibited-string cloud-sync
                     location] as a read-only archive." That
                     path has been retired and banned per
                     TB00000 v2_11/v2_12 §4.2 ABSOLUTE
                     STANDING RULE (OP-001 through OP-009).
                     The offending prohibited-string
                     reference was one of the two residual
                     active-governance contaminations
                     identified as OI-039 in TB00115.

                     VP-010 v1_1 rewrites the clause to
                     reference only the authoritative
                     path tree in TB00000 §11:

                       (a) Session logs and TB00000 are
                           retained locally alongside the
                           current version in
                           C:\Users\wstot\GitHub\TothBot\
                             TBXXXXX_Session_Logs\.

                       (b) All other controlled documents
                           have their version history
                           managed through git.

                       (c) The _Archive folder is reserved
                           for rt-state documents only.

                     Bill override: document control
                     constraints (v → rv → v transition
                     for content changes) are explicitly
                     overridden per Bill's directive on
                     04-22-2026, continuing the OI-039
                     remediation override scope. v1_0 →
                     v1_1 is a content clarification
                     (no scope change) qualifying as a
                     minor correction per VP-004 v → v
                     rule with Bill approval; rv state
                     not required. Approved by Bill.

  v1_0   04-04-2026  Promoted from dv to v. Corrected
                     VP-004 lifecycle transition rules per
                     Bill: dv → v is valid (direct promotion,
                     no rv step required for initial
                     development); rv state is for revising
                     approved documents only (v → rv → v);
                     removed incorrect dv → rv transition.
                     VP-005 updated: prohibited list now
                     correctly enumerates all state
                     transitions as BILL ACTION ONLY.
                     Approved by Bill.

  dv1_1  04-04-2026  DocPath correction: TothBot-V2-Docs corrected to TothBot_V2-Docs throughout.

  dv1_0  03-25-2026  Initial document. Phase 1 Step 1.8.
                     Version format definition. Lifecycle
                     state transition rules. WD Exception
                     versioning. Revision history requirements.
                     Filename version requirements. Superseded
                     document handling. 18 validation rules.

============================================================
END OF DOCUMENT
============================================================
